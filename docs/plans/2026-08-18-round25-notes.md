# Round 25+ working notes

## R25 STATUS: COMPLETE + PUSHED (commit 15d7581, 24 suites, 822 tests)

## R26 status: IN PROGRESS — greeting-triggered day preview
Implemented:
1. plan.js: RE_BRIEFING extended with question phrasings: how('s| is) (my day looking|today looking|today for me)|how does today look|what('s| is) the plan for today → notes:daily-briefing
2. dispatch-personal.js: greetSnapshot(briefing) — previews when name set + day has content; personality variants (concise/professional/playful/warm); R25 time-fact bridge renames day ('night today'); additive (empty → '')
3. dispatch.js greet case: text/narration = greetLine() + greetSnapshot(store.dailyBriefing()); detail.snapshot flag
4. harness: src/main/test-greeting-briefing.js (being finalized)

Store API reminders: addTask(text, {dueDate}), setTaskDone(id, done), setTaskDue(id, dueDate), addReminder(text, dueAt), dailyBriefing() → {dueToday, overdue, remindersToday}, all() → {tasks,notes,reminders}
greetLine signature: greetLine(personality, userName) exported from dispatch.js

TODO: finish harness, wire via python3 scripts/add-test-script.py test:greeting-briefing "node src/main/test-greeting-briefing.js", add .nova-greeting-test-data/ to .gitignore, npm test EXIT 0, README Round 26 section before Round 25 section, commit + push, report, continue loop

R26 harness env var: __NOVA_GREET_TEST; shim path: __dirname/../.. + shim-electron.js

## OLD R25 notes (reference)

## Status: was: IMPLEMENTED + TESTED, not yet committed (DONE)
- harness: `src/main/test-identity-briefing.js` — 46 PASS tests, EXIT 0
- module: `src/main/notes/dispatch-personal.js` — `personalizeNarration(kind, baseText)`, `applyTimePreference(items)`, `userFacts(limit=2)` (uses `userModel.relevantFacts(2)`)
- dispatch.js edits: briefing/digest cases now route narration through personalizeNarration with branch-matched base narrations (empty day/week get their own base line: "Nothing on the plate today — clear skies." / "Quiet week — nothing to report.")
- package.json: `test:identity-briefing` wired, now 24th suite; full chain 24 suites, 822 tests, EXIT 0
- .gitignore: added `.nova-identity-briefing-test-data/`, `identity.json`, `actions.log.json`
- README.md: identity table row updated (line 36) with personalization description

## Design decisions (bugs caught during dev)
1. Regex `\b(morning|...)\b` failed on plurals ("mornings") → `\bs?` added
2. Most recent time-of-day fact must win → scan reversed slice
3. Recap joins facts with '. ' → ends with period after last fact; test must not assert a '.' before the recap
4. Base narration must follow branch (empty vs full) for the spoken lead to match the text
5. Additive rule: empty name + empty facts → byte-identical pre-R25 narration; empty model tests compare full strings

## Test harness gotchas
- must `require("./notes/actions")` BEFORE first runNoteAction (else "Unknown action")
- harness require chain must set env vars + electron shim BEFORE requiring dispatch/identity/user-model
- dispatch-personal.js is loaded fresh only once per process (module cache)
- top-level await not allowed → wrap all await blocks in async IIFE
- file must not be treated as ESM; existing .js files in src/main run as CJS fine

## REMAINING STEPS for R25
1. Add "## Identity-aware briefing (Round 25)" section to README BEFORE the "## Nova identity (Round 24)" section (line ~295: "## Nova identity (Round 24)"), and append chain-count note: "The full chain is now **24 suites, 822 tests, EXIT 0**" + update Round 24 tail footnote if present
2. git add -A && git commit -m "Round 25: identity-aware briefing — userName/personality lead-ins + verbatim fact recaps woven into daily briefing & weekly digest narration, morning/afternoon/evening/night preference reorders reminders, additive (empty model = pre-R25 wording), zero outbound calls; 46 tests; 24 suites, 822 tests"
3. git push
4. Report, then start Round 26

## Round 26+ candidates
- Focus mode / Pomodoro: "Nova, focus mode for 25 minutes" — timer with task tracking
- Smart task prioritization by voice ("what should I work on first?")
- github-gem-seeker: open-source integrations for Nova
- manim-animator: animated explainer of Nova's architecture
- internet-skill-finder: trending AI assistant features
- Mood/energy check-in stored in user model, affects briefing tone
