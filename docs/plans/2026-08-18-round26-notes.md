# Round 26 working notes — greeting-triggered day preview

## What's implemented (code done, harness debugging in progress)
1. `src/main/notes/plan.js`: RE_BRIEFING extended with question phrasings → notes:daily-briefing:
   `how('s| is) (my day looking|today looking|today for me)|how does today look|what('s| is) the plan for today`
2. `src/main/notes/dispatch-personal.js`: `greetSnapshot(briefing)` exported (with userFacts). Logic:
   - no userName or empty briefing → "" (additive)
   - pieces: `${nDue} thing(s) on the plate`, `${nOv} overdue`, `${nRem} reminder(s)${nDue||nOv ? " today" : ""}` (the " today" tail drops on reminder-only plates to avoid "today has X today")
   - concise: ` — ${summary}.` / professional: ` — ${dayWord}today: ${summary}.` / playful: ` — the ${dayWord}cosmos wrote you ${summary}!` / warm(default): ` — ${dayWord}today has ${summary}.`
   - R25 bridge: time-of-day fact renames dayWord ("night today")
3. `src/main/notes/dispatch.js` greet case: `text = greeting + greetSnapshot(store.dailyBriefing())`, `narration` same, `detail.snapshot: !!preview`. greetLine(personality, userName) still from dispatch.

## Harness: src/main/test-greeting-briefing.js
- 24 PASS in the first section (question phrasings 9 + negatives 4 + legacy 1 + snapshot 10)
- IMPORTANT gotcha #1: top-level await makes Node 22 parse file as ESM (silently!). All await blocks MUST be inside async IIFE. Fixed.
- Gotcha #2: store.all() returns a SNAPSHOT (copy) — mutations on it don't stick; must use store.deleteTask(id)/cancelReminder(id)
- Gotcha #3: cancelReminder sets fired=true but fired reminders still count in dailyBriefing remindersToday ("if it rings today, it belongs in the briefing")
- Gotcha #4: userModel.resetForTesting() needed between sections — facts last-wins and bleed otherwise
- Gotcha #5: harness env: __NOVA_IDENTITY_TEST/__NOVA_USER_MODEL_TEST = DATA_DIR; store.setStorePathForTesting(path.join(DATA_DIR,"nova-notes.json"))
- Remaining failure at line ~169 (e2e dispatch section): need to check — previous run died there. Assertion labels in that section: "greeting fires end-to-end", "empty day: no snapshot flag", "greeting + due task: preview appended" expected text `base + " — today has 1 thing on the plate."`, "snapshot flag on populated day", "narration mirrors the previewed line", "greeting line comes first, preview as tail", "no name → byte-identical pre-R26 greeting on a populated day" (x2), "greeting path makes zero outbound calls". NOTE: after single-item block we deleted tasks; the e2e block re-adds "finish report" due today — but reminder 'stand up' was cancelled in the previous section and identity was reset? The e2e section sets userName Alex warm again but does NOT reset userModel; the time fact 'I work at night' may still be present → snapshot text would contain 'night today'. ALSO cancelled reminder with fired=true still counts in remindersToday. Fix: in the e2e block, add userModel.resetForTesting() and clear store (deleteTask all / cancelReminder) before re-adding, OR adjust expected strings.
- After e2e: 25th label = no-outbound checks (2 await calls) + completion log "All Round 26 greeting-briefing tests passed (${pass})."

## Current debugging state (as of latest run)
Harness first section: ALL 24 PASS (24 PASS lines confirmed; harness exit was via the e2e section).
E2E section error: `ReferenceError: greetSnapshot is not defined` — caused by the local `function greetLine(personality, userName)` defined at the BOTTOM of the file (lines 197+) which Node's hoisting evaluates... actually the error is 'greetSnapshot is not defined' — meaning some line references greetSnapshot bare. The local greetLine at bottom was written as a COPY of dispatcher's greetLine but possibly references greetSnapshot OR the test expects greetLine(...) but the harness's own greetLine exists at bottom only — check bottom of file. Wait the ACTUAL error: 'greetSnapshot is not defined' at line 169 — line 169 is `assert(!r0.detail.snapshot, ...)` — the IDENTIFIER is `r0.detail.snapshot` which can't throw that. Hmm — maybe the real culprit: the harness defines local `greetLine()` at bottom but inside e2e I call `greetLine("warm", "")` — that works if defined. The error 'greetSnapshot is not defined' is thrown because... dispatch-personal.js module exports greetSnapshot? Check module.exports at bottom of dispatch-personal.js — perhaps it exports greetLine from dispatch? Simplest: just check what the bottom of harness defines and what dispatch-personal exports.
Plan to fix e2e section:
1. identity.set({userName:'Alex',personality:'warm'}); userModel.resetForTesting(); also clear store (loop deleteTask on all tasks, cancelReminder all) — previous sections left 'finish report' undone + cancelled 'stand up' reminder counting
2. 'hey nova' greeting: snapshot text depends on reminder fired state — after clearing reminders it's ' — today has 1 thing on the plate.' (warm)
3. local greetLine helper at bottom must match dispatcher's EXACT wording (concise/professional/warm/playful) — it currently does (copied). OK.
4. no-outbound section needs fetch stub set BEFORE the await calls — currently stub + asserts live in a plain {} block inside the IIFE; fine, but fetch must not have been set earlier (earlier sections don't stub it). NOTE: globalThis.fetch stub persists across sections — the third section sets it; the second e2e block then uses it fine. BUT third section does fetch stub after e2e — order is e2e first, then no-outbound. Fine.

## STATUS UPDATE (phase 2)
- Harness DONE: 33 PASS, "All Round 26 greeting-briefing tests passed (33)."
- Wired: test:greeting-briefing added via script; .nova-greeting-test-data/ in .gitignore
- FULL CHAIN: EXIT 0, 855 PASS, 25 suites (822+33)
- REMAINING: README — insert "## Greeting-triggered day preview (Round 26)" section BEFORE "## Identity-aware briefing (Round 25)" (line ~295 area, before "## Nova identity (Round 24)" at 297); update R25 section chain footnote '24 suites, 822 tests' → '25 suites, 855 tests'; update feature table? optional. Then git add -A, commit, push, report, continue.
- README R25 section location: grep -n '## Nova identity (Round 24)' → 297; identity table row ~line 37 (grep '^| Nova identity')

## To do
1. Fix e2e section expectations/state isolation (reset userModel + clear store, or expect 'night today')
2. Run full harness → all pass with completion line
3. python3 scripts/add-test-script.py test:greeting-briefing "node src/main/test-greeting-briefing.js"
4. grep -qx ".nova-greeting-test-data/" .gitignore || echo >> .gitignore
5. npm test → EXIT 0; count = 822 + 25(ish) = 847
6. README: "## Greeting-triggered briefing (Round 26)" section BEFORE "## Identity-aware briefing (Round 25)"; update footnote on Round 25 section chain count to 24 suites 822; feature table? maybe skip (greeting preview is enhancement of identity row already)
7. git commit -m "Round 26: greeting-triggered day preview — \"good morning\" now opens with your plate (counts, all 4 personalities, time-of-day fact bridge, additive when no name/empty day), plus 9 new question phrasings (\"how's my day looking\", \"what's the plan for today\") routing to the daily-briefing gate; 25 tests; 25 suites, 847 tests" && git push
8. Report + start Round 27

## Round 27+ candidates
- Focus mode / Pomodoro timer with task tracking ("Nova, focus mode for 25 minutes")
- Smart task prioritization by voice ("what should I work on first?")
- github-gem-seeker: open-source integrations for Nova
- manim-animator: animated architecture explainer
- internet-skill-finder: trending AI assistant features
- Mood/energy check-in stored in user model, affects briefing tone

# Round 27 design (mood/energy check-in) — WORK IN PROGRESS

Design (lightweight, local-only, no model):
1. plan.js: new `RE_MOOD_CHECK` BEFORE identity ask branch: /^(?:nova\s*,?\s*)?(?:how am i feeling today|how am i doing|how do i feel|check in with me|what's my mood|what is my mood today)\s*$/i + extraction regex `RE_MOOD` for explicit statements "i feel ..." / "i'm feeling ...".
   - Simplest robust scope: one action `notes:mood-check` (no payload) that reads last mood; and "i feel X"/"i'm feeling X" piggybacks on RE_REMEMBER_FACT (it already starts with "remember..."? NO — "i feel tired" lacks "remember". Add separate RE_MOOD_STATEMENT: /^(?:nova\s*,?\s*)?(?:i feel|i'm feeling|i am feeling|i've been feeling|i've been)\s+.+\S\s*$/i → notes:mood-statement {mood}. Also "today i feel X" variant.
2. actions.js: register notes:mood-check L1, notes:mood-statement L1 (both SAFE — store only).
3. user-model.js: NO new API — mood statements are just facts ("I feel energetic today") added via addFact(); mood-check reads the most recent fact matching MOOD_FACT_REGEX (feel|feeling|feels|tired|energized|exhausted|stressed|great|down|off|amazing|meh|burned out).
4. dispatch.js: mood-check case → returns {ok:true, text:"You told me: \"…\" 2 hours ago. I've noted it in your briefing.", narration}, last mood + age ("just now"/"X hours ago"); if none → "You haven't told me yet — try \"I feel energetic today\"."
5. dispatch-personal.js: extend userFacts/personalization so briefing/digest/greeting lead-in gets a mood-aware prefix when a same-day mood exists: "You sounded energized this morning — here's your day: …" (additive). Only the SAME DAY's last mood counts (mood is ephemeral).
6. Harness test-mood-check.js (~30 tests): routing 6 phrases + negatives, statement extraction 5 phrases, dispatcher wording no-mood/recent/aged, additive rules (greeting unaffected when no mood, briefing lead-in with mood), persistence, zero outbound.
7. Note: greeting already carries snapshot; mood prefix should sit in greetSnapshot? Keep separate: mood prefix in the greeting only when mood present AND name? No — mood prefix is independent of name. greetSnapshot returns ""; greeting text = greetLine + preview + (mood prefix?). Order: greeting + moodPrefix(personality, mood) + preview. Additive both.
Chain after: 26 suites, ~885 tests.

## R27 PROGRESS (update 2)
- plan.js DONE: RE_MOOD_CHECK + MOOD_LEXICON + RE_MOOD_STATEMENT routed BEFORE greet (mood check lines after RE_GREETING block, before RE_NOTE). Order in file: RE_GREETING case → mood-check → mood-statement → RE_NOTE. All 9 question routes + statement routes + negatives PASS.
- actions.js DONE: notes:mood-check L1 + notes:mood-statement L1 registered.
- dispatch-personal.js DONE: latestMood(), moodAge(updatedAt, now), moodNarration(now) → "You mentioned ${age} that \"${fact}\" — ", moodGreet(now) → personality variants: concise `(${age}: "${fact}") — `, professional `I have your ${age} check-in on file ("${fact}") — `, playful `the cosmos heard you ${age} ("${fact}") — `, warm `you told me ${age} ("${fact}") — `. Exports added.
- dispatch.js DONE: briefing narration = personalizeNarration(...).replace(/^(\s*)/, m => m + moodNarration()); same for digest; greet narration = greeting + moodGreet() + preview (text unchanged). Cases notes:mood-check (no mood → "You haven't told me..." with suggestion; personality ack: concise "Latest check-in: ... (recorded X ago)", professional "Here's your latest check-in:", playful "The cosmos remembers:", warm "You told me, X:") and notes:mood-statement (detail.ok===false error text; ack: concise "Noted.", professional "Logged — I'll keep it in mind for your briefings.", playful "The cosmos heard you! ✨", warm "Got it — I'll keep that in mind today.") + exports latestMood/moodAge/moodNarration/moodGreet.
- Harness test-mood-check.js: 25 routing asserts PASS. REMAINING BUG: require("../permissions/action-registry") wrong path — harness is in src/main so it should be "./permissions/action-registry". Fix that, then re-run.
- Harness design: NOW=2026-08-18T10:00Z; setStorePathForTesting/identity.resetForTesting/userModels.resetForTesting("/tmp/.nova-mood-test-data"); identity.set({userName:'Alex',personality:'warm'}). Sections: routing done; e2e registry check → fix path; mood-check empty; add mood-statement fact + patch updatedAt to NOW-20min; check-in 20 minutes ago; moodAge assertions; personality acks (switch identity.set personality); latest-mood math (remember-fact 'I feel calm now' patch updatedAt NOW-2min, forget restores); weaving b1/d1/g1 with mood; additive b2/d2/g2 after forget; no-outbound fetch stub.
- After harness green: add-test-script.py test:mood-check; .nova-mood-test-data/ to .gitignore; full chain (expect 26 suites ~885 tests); README insert "## Mood/energy check-in (Round 27)" before "## Greeting-triggered day preview (Round 26)" section (grep: line ~293); update chain footnotes 25/855 → 26/885; commit+push; report; next round.
- README R27 section content: two actions (mood-check L1 read latest mood with age; mood-statement L1 stores fact with dedicated ack), MOOD_LEXICON gating avoids swallowing "I am a developer" notes, weaving into briefing/digest/greeting narration only (text untouched), additive guarantee, zero outbound, harness covers routing/negatives/age math/personality acks/latest-wins/forget-revert/additive/no-outbound.

## R27 DEBUG FINDINGS (update 3)
- moodAge math confirmed: minutes = floor(ms/60000); <2 → just now; <60 → minutes; <12h → hours; else day-part. For "1 minute ago" use 65000-119999ms window (115000 OK: floor(115/60)=1 → "1 minute ago" — but DBG showed "just now"?? floor(115000/60000)=1.9→1. Should NOT be just now. DBG output said "just now" — SUSPICIOUS: maybe module cached old code? No — code right. WAIT: DBG uses NOW2=2026-08-19T08:00Z minus 115000 → ms=115000 → minutes=1 → "1 minute ago". DBG printed "just now"?! Unless... the DBG line executed with NOW2 different? floor(115/60)=1 <60 → returns "1 minute ago". The only way to get "just now" is minutes<2 → minutes must be 0 or 1 → 1<2 → YES 1<2 is TRUE → just now! threshold is minutes < 2 → "1 minute ago" (1 min) falls into <2 → "just now". Code treats 1-minute-old as just now. So my test expectation wrong again: minutes<2 includes 1 minute. Fix expectation: "moodAge: 1 minute ago" → expect "just now"; or change to test 5 minutes ago (use 300000ms). Simplest: use 300000 (5min) → expect "5 minutes ago".
- b1.narration DBG shows mood IS woven: 'You mentioned just now that "I feel energized today" — Alex, here's your day: Nothing on the plate today — clear skies. Remembered about you: I feel energized today.' — my assertion looked for "I feel calm now" but the FORGET of 'I feel calm now' happened AFTER the brief... actually order: add 'I feel calm now' (ts in model), then assertions... DBG shows "I feel energized today"! Because latestMood picks newest: 'I feel calm now' was added at line 161, then forget at line 165 ('forget that I feel calm now'), then b1 briefing at 183 — calm was forgotten, energized remains! So harness flow bug: weaving tests run AFTER the forget. Move weaving block BEFORE the latest-mood math section (swap sections 2c and 3). Then assertions for 'I feel calm now' valid.
- After swap: latest-mood math section must re-add 'I feel calm now' for its assertions (it already does at line ~161, fine after swap).

# Round 28 design (mood-aware task prioritization) — WORK IN PROGRESS
Status R27 DONE: commit b54222d, 26 suites, 919 tests (855+64), README sections R27/R26/R25/R24 all updated, feature table row 36 updated.
R28 design (lightweight, local-only, no model):
1. plan.js: new `RE_PRIORITY_CHECK` before RE_NOTE: /^(?:nova\s*,?\s*)?(?:what should i work on first|prioritize my tasks|what('s| is) most urgent|what('s| is) the most important|help me prioritize|what comes first|order my tasks)\s*$/i → notes:priority-check (no payload).
2. actions.js: register notes:priority-check L1 SAFE.
3. dispatch.js: new case notes:priority-check:
   - pending = store.all().tasks pending (no done), compute overdue by dueDate < today; now = Date.now().
   - mood = latestMood(); lowEnergy = MOOD_FACT_REGEX low-energy words (tired/exhausted/stressed/drained/fatigued/down/burned out — reuse: define LOW_ENERGY_RE in dispatch-personal.js).
   - rank: overdue tasks first (oldest first), then due-today soonest, then rest; low-energy mood tiebreaks rest by length (shortest first, token count) — small tasks first when tired.
   - empty: "Nothing to prioritize — your plate is clean."
   - narration: mood-framed opener when mood recent (<12h?): "You mentioned this morning you're feeling tired — so small wins first: …" vs normal "Here's what I'd work on first: …"
   - text: same plain list: "1. X (overdue). 2. Y (due today). 3. Z." with up to 5 shown.
4. dispatch-personal.js: add `prioritize(tasks, now)` → {order:[task...], moodFlag} using store task objects {text, done, dueDate?}. Keep pure (no require of store — caller passes tasks).
5. Harness test-mood-priority.js (~45 tests): routing 8 phrases + 4 negatives (no mood-check collision, no briefing collision), empty plate, single task, ordering math (overdue>due-today>rest, oldest-first overdue, soonest-first today), low-energy shortest-first within "rest" bucket, high-energy mood doesn't reorder rest, mood-framing narration when mood recent + not when stale (>12h), no mood → same order as low-energy? (deterministic: low-energy changes only rest-bucket tiebreak by length — verify normal mood sorts rest by insertion; keep simple: normal = insertion order, low = shortest first), additive guarantees, zero outbound.
6. Same wiring steps as R27: add-test-script.py test:mood-priority; .nova-mood-priority-test-data/ gitignore; npm test; README section "## Mood-aware prioritization (Round 28)" before "## Mood/energy check-in (Round 27)"; footnotes chain → 27 suites, 9NN tests; feature table row 36 tail add ", and mood-aware prioritization ('what should I work on first?' sorts overdue→due-today→rest, low-energy moods surfacing small wins first)".
Harness header: copy from test-mood-check.js (same env/shim/assert/reset pattern); DATA_DIR=/tmp/.nova-mood-priority-test-data; tasks via store.addTask(text,{dueDate}) — setTaskDone(id) to remove from pending.

## R28 STATUS: DONE (implementation + harness green)
- Files: plan.js (RE_PRIORITY_CHECK with optional 'task' and 9 phrases + nova prefix), actions.js (notes:priority-check L1 SAFE), dispatch-personal.js (LOW_ENERGY_RE + prioritize(tasks,now) pure fn), dispatch.js (case notes:priority-check; effPayload pins opts.now for due-date math; Date.now() for mood recency <12h; empty plate / mood-framed opener / plain opener / 5-item cap + tail / labels).
- user-model.js bugfix + seam: updatedAt now set on FIRST fact creation (R27 moodAge relied on it) + setNowForTesting(d) test pin for fact timestamps.
- Harness test-mood-priority.js: 66 tests, NOW pinned 2026-08-19T10:00Z. Sections: routing 11 positives + 7 negatives, registry L1, prioritize math (empty/null/single/ladder/rest untouched/low-energy rest shortest-first/older-overdue never outranked/day-boundary minute-before-midnight vs next-minute/future-due), dispatcher wording (empty plate, no mood, low-energy mood-framed, stale >12h, 5-cap tail, all-personalities additive, 'I feel okay' non-reorder), zero outbound.
- Wiring: test:mood-priority via add-test-script.py; .gitignore entry; npm test full chain EXIT 0, 985 PASS, 27 suites.
- REMAINING: README insert "## Mood-aware prioritization (Round 28)" before "## Mood/energy check-in (Round 27)"; update R27/R26/R25 chain footnotes to 27 suites 985 tests; feature table row 36 tail; git add/commit/push; report; next round.
- README R27 section location: grep -n '## Mood/energy check-in' README.md; R26 section: '## Greeting-triggered day preview'; R25 '## Identity-aware briefing'; R24 '## Nova identity (Round 24)'. Feature table identity row: grep -n 'Nova identity' README.md.


# Round 29 design (focus mode / Pomodoro timer) — WORK IN PROGRESS

Status R28 DONE: commit f7cecd8, 27 suites, 985 tests (919+66), README updated, pushed.

R29 design (local-only, no model, ties into tasks + notifications + action log):
1. plan.js: new `RE_FOCUS` before RE_NOTE: /^(?:nova\s*,?\s*)?(?:focus mode(?: for (\d+)(?: minutes?| min))?|start a pomodoro(?: for (\d+)(?: minutes?| min))?|focus for (\d+)(?: minutes?| min)|start (\d+)-minute(?: focus)?|pomodoro(?: for (\d+)(?: minutes?| min))?|start focus|work for (\d+) minutes?)\s*$/i → notes:focus-start {durationMin} (default 25); also start/end/stop variants → notes:focus-stop (no payload).
2. store.js: new focus session records appended to userData/notes.json: {id, durationMin, startedAt, stoppedAt?, status: "running|completed|cancelled"}. API: startFocus(durationMin, now?), stopFocus(now?) (stops latest running), focusHistory(n), latestFocus(), cancelFocus now = stop.
3. actions.js: notes:focus-start L1 SAFE (creates local record + speaks acknowledgment; NO timer side-effects in main — renderer shows countdown via event nova:focus-started), notes:focus-stop L1 SAFE.
4. dispatch.js: focus-start case → narration: "Focus mode on — X minutes, from HH:MM to HH:MM. When it ends I'll ping you — and remember, 'what should I work on first' is ready whenever you need a plan." + detail {id, durationMin, endsAt}. Stop case → "Focus ended after X minutes — want me to mark a task done?" suggestion when pending tasks exist.
5. Renderer: HUD countdown chip (cyan border pulse) during running session, bell + notification when done. IPC events nova:focus-started / nova:focus-stopped.
6. Harness test-focus-mode.js (~60 tests): routing (start phrases with/without duration, default 25, variants like '30 minutes', '2-hour'→120?, negatives), store math (append, stop latest-running only, completed/cancelled records persist, latestFocus null when none), dispatcher wording (start with default/custom duration, end time computed, stop with duration spoken, suggestion when pending tasks, no suggestion on empty plate), all-personalities additive, zero outbound.
7. Wire: add-test-script.py test:focus-mode; .gitignore data dir; npm test; README insert "## Focus mode / Pomodoro (Round 29)" before "## Mood-aware task prioritization (Round 28)"; footnotes → 28 suites, ~1045 tests; feature table row add or task-stats row tail.
Harness header: copy from test-mood-priority.js; DATA_DIR=/tmp/.nova-focus-test-data; harness NOW pinned (setStorePathForTesting + identity reset; no pc.now needed — focus uses wall clock for endsAt, harness accepts 'ends in ~' tolerance via regex on HH:MM).

## R29 PROGRESS LOG (keep updating)
- Store DONE: store.js has focus array + startFocus(durationMin, now?) (600-min cap, running-session swap cancels old candidly), stopFocus(status='completed'|'cancelled', now?), focusHistory(limit), latestFocus(), focusMinutesThisWeek(now), setNowForTesting(d), resetForTesting wipes focus. Smoke test passed (swap→cancelled hist, minutes=50).
- Plan DONE: RE_FOCUS_START (wrapped outer (?:…) so ^/$ anchor all branches; branch2 = 'start (?:a )?pomodoro(?: for N min)?' kept together) + RE_FOCUS_STOP; routing: STOP checked BEFORE START; duration extraction mm[1..6] with hourUnit test on full sentence t. Verified routing: start variants (default 25), minutes/hours/decimal hour all correct, negatives (note focus mode→add-note, add focus mode→add-task, 'five minutes'→null, 'end focus mode for 30 minutes'→null, mood-check untouched).
- TODO next: actions.js register notes:focus-start + notes:focus-stop (L1 SAFE); dispatch.js cases (start: narration with endsAt HH:MM in local time + duration spoken; stop: ended-after wording + suggestion 'mark a task done?' when pending exist, no suggestion empty plate; effPayload now injection); preload.js onFocusStarted/onFocusStopped events; renderer app.js HUD chip + toast; harness test-focus-mode.js (~60 tests, pattern = test-mood-priority.js); wire add-test-script.py test:focus-mode; gitignore; npm test → expect 28 suites ~1045 tests; README insert section before '## Mood-aware task prioritization (Round 28)'; footnotes; commit+push; report; next round.
- Design decisions locked: focus is LOCAL RECORD + renderer countdown (main emits nova:focus-started/stopped events; actual ticking done in renderer). Store API owns truth. Dispatcher wording default personality warm but text identical across personalities in test (narration uses personalizeNarration? — keep text same for all, narration personality-tinted only where existing personalizeNarration allows). Keep simple: dispatcher text = end-time line; narration same.
- Harness: DATA_DIR=/tmp/.nova-focus-test-data, notes path path.join(DATA_DIR,'notes-focus.json'), identity/userModel set env __NOVA_IDENTITY_TEST/__NOVA_USER_MODEL_TEST before requires; assert(label, cond); async IIFE; no top-level await.


# ROUND 29 WORKING NOTES (focus mode / Pomodoro) — status: harness debugging, not yet green

## R28 completion recap
Commit f7cecd8: 27 suites, 985 tests, EXIT 0. README R28 section inserted before R27; footnotes updated.

## R29 implementation (all syntax-checked, smoke-verified)
- store.js: focus API — startFocus(durationMin, now?), swap cancels old running; stopFocus(status, now); focusHistory(limit); latestFocus(); focusMinutesThisWeek(now); resetForTesting wipes focus. Cap 600 min (10h).
- plan.js: RE_FOCUS_STOP checked BEFORE RE_FOCUS_START (both before RE_NOTE/priority). RE_FOCUS_START groups mm[1..7]; default 25; hour unit via /\b(h|hour|hours)\b/i on sentence; 'start a focus session' works; negatives verified: 'note focus mode'→add-note, 'I feel focused'→mood-statement, 'focus on the report'→null.
- actions.js: notes:focus-start L1 (payload.durationMin; calls store.startFocus(durationMin, pc.now)); notes:focus-stop L1 (store.stopFocus("completed") + pendingCount).
- dispatch.js: effPayload pins now for focus start/stop; focus-start text "Focus mode on — N minutes/2 hours, HH:MM to HH:MM … what should I work on first is ready"; detail.session {id,durationMin,startedAt,endsAt}. focus-stop: real elapsed mins ("a minute" singular), suggests task done when pending>0; no-session: "No focus session was running — say "start focus mode" whenever you want…".
- preload.js: onFocusStarted/onFocusStopped listeners (nova:focus-started/stopped). main.js emits after runNoteAction success.
- index.html: focus-chip span (focusChip/focusChipLabel) in topbar-right. app.js: el registry, renderFocusChip folded into tickClock; state.focusSession+toast; chip click → runNotesCommand("stop focus"). hud.css: .focus-chip/.focus-dot (pulse)/.focus-chip-done (amber).
- action-log.js: env seam __NOVA_ACTION_LOG_TEST → log file under that dir.
- test-focus.js harness written (~67 asserts): routing 13+7, duration 11, store math (swap/history/pin/cap), dispatch wording, log 3 + zero outbound 2. Fixes applied: 16:15→4:15 PM local match; __NOVA_ACTION_LOG_TEST set + actionLog.clear() after fresh state; swap-cancel assertion checks outcome==='cancelled'.
- R28 harness bugfix shipped in R29: user-model.js addFact stamps updatedAt on FIRST create (R27 age wording silently depended on it).

## Remaining R29 steps
1. Re-run test-focus.js → all PASS (2 failures were the 16:15 and swap-cancel ones, now fixed in code)
2. python3 scripts/add-test-script.py test:focus "node src/main/test-focus.js"
3. .gitignore: .nova-focus-test-data/
4. npm test → /tmp/testlog29.txt, EXIT 0
5. README: insert "## Focus mode / Pomodoro (Round 29)" BEFORE "## Mood-aware prioritization (Round 28)"; footnotes → 28 suites, ~1052 tests (985+67); feature table row 36 tail update.
6. git commit+push, report, start R30.

## R30+ candidates
- github-gem-seeker integrations; manim-animator architecture video; natural language task search ("find tasks about the report"); "plan my day" time-blocked schedule from tasks+mood; focus stats in side panel; focus minutes in daily briefing.


# ROUND 30 DESIGN — natural language task search

## Goal
"find tasks about the report", "search my tasks for client", "show tasks with billing" → search the task list with fuzzy/stemming token matching. Fully local, L1 SAFE read.

## Implementation
1. `store.js`: add `searchTasks(query, opts={includeDone:false, now:Date.now()})` → matches array `{task, reason, score}`. Tokenize query (split non-alnum, lowercase, drop stop tokens: the/a/is/for/of/about/with/in my tasks show), strip stopper tail "find tasks"/"search my tasks" (already stripped in plan). Scoring: full-word (i18n: use includes on normalized tokens) = 10 per token; substring = 5; done-flag: exclude done unless opts.includeDone. Order: score desc, then most-recently-updated desc. Note search-tasks matches the *task text* only (notes are searched by existing search-notes keyword search). Cap results 10.
2. `plan.js`: RE_TASK_SEARCH before RE_NOTE: /^(?:nova\s*,?\s*)?(?:find tasks|search tasks|find (?:my )?tasks about|search (?:my )?tasks for|tasks about|tasks for|look for tasks|show tasks with)(?:\s+(.+?))?\s*$/i — payload: {query: stripped} → notes:task-search. Negatives to keep out: "find my task list" (task-list), "show all tasks", "my tasks", "what's on my task list" (stay task-list), "find the task about x" (task-search actually OK), "tasks with notes" → hmm must not collide with search-notes; keep RE_SEARCH_NOTES ("find note", "search notes") checked FIRST in dispatch order (it already is, in notes region before tasks). Add negative 'find a note' → notes:search-notes.
3. `actions.js`: notes:task-search L1 SAFE; execute: store.searchTasks(payload.query); also return counts via store.
4. `dispatch.js`: notes:task-search case — formatting like priority-check: "Here's what I found for 'X':" + numbered list with due badges (overdue/due today due tags, done flag), empty: "Nothing matches 'report' in your tasks." Narration same.
5. Harness `test-search-tasks.js` (~50): routing 10+ negatives 6, tokenization/scoring (full vs substring, stop token drop, 'about' stopper), ordering, includeDone exclusion, cap, badges wording, empty, zero outbound, additive (text field plain, narration same), registry level.
6. Wire: add-test-script.py test:task-search; .gitignore .nova-task-search-test-data/; npm test; README section before R29; footnotes → 29 suites, ~1100 tests.

## R30 PROGRESS SNAPSHOT (updated before compaction)
DONE: store.js searchTasks(query, opts={includeDone}) added + exported (score: full-word=10 via word-boundary regex, substring=5; stop-token stripping; recency tiebreak; cap 10) — smoke tested OK. plan.js: RE_TASK_SEARCH def + routing inserted BEFORE RE_TASKS_LIST (tail after about/for/with/matching; preposition-only tails → error). actions.js: notes:task-search L1 registered after list-tasks. dispatch.js: notes:task-search case inserted after list-tasks — numbered results + due badges (NOTE: my draft used nonexistent dueLabel() — REPLACED with priority-check-style safeDay/todayMs tag = overdue/due-today only, no 'due N' third label, keeping it simple & bug-free). ALSO fixed pre-existing latent bug: plan.js line ~617 RE_IMPLICIT_SET_DUE block — `const subj` was block-scoped, fallback `text: subj || t` threw ReferenceError when ctx.tasks empty ('tasks due today' crashed pre-R30). Hoisted subj outside the if-block. Smoke test all-variants passed (routing positives/negatives/notes-search/list separation).
TODO: write test-search-tasks.js (~50): routing 10+ negatives 6, stop-token stripping, whole-word>substring scoring, done-exclusion, includeDone opt, recency tiebreak, cap 10, empty→'No tasks match "X"', badges wording, zero outbound, registry L1. Pattern from test-mood-priority.js/test-focus.js (env vars __NOVA_... at top BEFORE requires, store DATA=/tmp/.nova-task-search-test-data/n.json, resetForTesting before seeding, async IIFE, assert fn, no top-level await).
THEN: add-test-script.py test:task-search; .gitignore .nova-task-search-test-data/; npm test > /tmp/testlog30.txt EXIT 0; README section '## Natural language task search (Round 30)' BEFORE '## Focus mode / Pomodoro (Round 29)' line ~297; footnotes → 29 suites, ~1102 tests (1052+50); feature table row 36; commit+push; report; start R31.
R31 candidates: natural language note search extension, focus stats in side panel, 'plan my day' time blocks, gem-seeker integrations.

## R30 STATUS: DONE (implement+test+README)
Harness test-search-tasks.js GREEN: 45 tests, EXIT 0. Full chain npm test EXIT 0, 1,097 tests, 29 suites (test:task-search wired via add-test-script.py, .gitignore has .nova-task-search-test-data/). README updated: new '## Natural language task search (Round 30)' section before R29, all chain footnotes → 29 suites 1,097 tests, feature table row 36 tail mentions natural language task search.
REMAINING: git add -A && commit -m "Round 30: natural language task search…" && push → report → R31.

## R30 DEBUG STATE (past)
Harness test-search-tasks.js written, body in async IIFE. 3 failures to fix:
1. R24: dispatcher text shows 'finish the client report' ranked FIRST but matches[0].score=10 not 20?? No — output ranked 'client report follow-up meeting' first (score 10) above 'finish the client report' (20) → store sorting bug OR score calc: 'client report' query tokens [client,report]; 'finish the client report' → client(10)+report(10)=20. 'client report follow-up meeting' → client(10)+report(10)=20 too! tie → recency: 'client report follow-up meeting' added LAST → first. So assertion 'rank first + score 20' is half right. FIX: assert first item score=20 AND text one of the two 20-score tasks; or add a task 'client' without report (10) to prove ordering. Better: make R24 assert scores[0]>=20 and matches[0].task.text includes 'client' && includes 'report'.
2. R29: 'find tasks about' now routes to task-search (regex matches it: tail 'about' then strip → query '') → payload {query:''} → dispatcher gets matches [] but payload.query='' → text 'No tasks match "".'; run() text null?? Actually text=null because plan returns error (query strip in plan: raw.replace preposition → '' → planning error returned). So run().text=null → assert startswith throws. FIX: assert R29 expects res.text=null and res.ok===false and res.text planning error? Actually plan returns {error:...} so runNoteAction returns ok:false text=error. Update assertion: !res.ok && res.text starts with 'Find tasks about what'.
3. R34: due-today badge — t2 added with dueDate=new Date().toISOString() but done=true → done excluded from results (correct R36). R34 regex /team standup \(due today\)/ fails because the task is done. FIX: make t2 pending + add separate done task t2d; R36 asserts done-task excluded.
Store note: test data /tmp/.nova-task-search-test-data/notes.json. Harness pattern: env seams → requires (actions,dispatch,plan,store,identity,user-model) → fetch stub → IIFE body → })().catch(process.exitCode=1).
Also REMEMBER: after fixes → add-test-script.py test:task-search; .gitignore .nova-task-search-test-data/; npm test > /tmp/testlog30.txt EXIT 0; README section before '## Focus mode / Pomodoro (Round 29)' (line ~297); footnotes 29 suites ~1102 tests; feature table row 36; commit+push; report; next round R31.
R31 candidates: note search parity (natural language for notes), focus stats side panel, 'plan my day' time blocks, gem-seeker integrations.

## R30 gotchas from R29
- RE_* regexes: wrap ALL alternations in a single non-capturing group with ^...$ anchors around it.
- Module caching in tests: clear() must come BEFORE require or env seam set at very top; action-log entries written DURING later blocks; read log LAST.
- setStorePathForTesting resets in-memory data — call before seeding.
- store2.json file path (not dir) for DATA_DIR env.
- userModels list() snapshot; setNowForTesting exists for fact timestamps.
