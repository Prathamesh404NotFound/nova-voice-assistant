# Round 26 working notes — greeting-triggered day preview

## What's implemented (code done, harness debugging in progress)
1. `src/main/notes/plan.js`: RE_BRIEFING extended with question phrasings → notes:daily-briefing:
   `how('s| is) (my day looking|today looking|today for me)|how does today look|what('s| is) the plan for today`
2. `src/main/notes/dispatch-personal.js`: `greetSnapshot(briefing)` exported (with userFacts). Logic:
   - no userName or empty briefing → "" (additive)
   - pieces: `${nDue} thing(s) on the plate`, `${nOv} overdue`, `${nRem} reminder(s)${nDue||nOv ? " today" : ""}` (the " today" tail drops on reminder-only plates to avoid "today has X today")
   - concise: ` — ${summary}.` / professional: ` — ${dayWord}today: ${summary}.` / playful: ` — the ${dayWord}cosmos wrote you ${summary}!` / warm(default): ` — ${dayWord}today has ${summary}.`
   - R25 bridge: time-of-day fact renames dayWord ("night today")
3. `src/main/notes/dispatch.js` greet case: `text = greeting + greetSnapshot(store.dailyBriefing())`, `narration` same, `detail.snapshot: !!preview`. greetLine(personality, userName) still from dispatch.

## Harness: src/main/test-greeting-briefing.js
- 24 PASS in the first section (question phrasings 9 + negatives 4 + legacy 1 + snapshot 10)
- IMPORTANT gotcha #1: top-level await makes Node 22 parse file as ESM (silently!). All await blocks MUST be inside async IIFE. Fixed.
- Gotcha #2: store.all() returns a SNAPSHOT (copy) — mutations on it don't stick; must use store.deleteTask(id)/cancelReminder(id)
- Gotcha #3: cancelReminder sets fired=true but fired reminders still count in dailyBriefing remindersToday ("if it rings today, it belongs in the briefing")
- Gotcha #4: userModel.resetForTesting() needed between sections — facts last-wins and bleed otherwise
- Gotcha #5: harness env: __NOVA_IDENTITY_TEST/__NOVA_USER_MODEL_TEST = DATA_DIR; store.setStorePathForTesting(path.join(DATA_DIR,"nova-notes.json"))
- Remaining failure at line ~169 (e2e dispatch section): need to check — previous run died there. Assertion labels in that section: "greeting fires end-to-end", "empty day: no snapshot flag", "greeting + due task: preview appended" expected text `base + " — today has 1 thing on the plate."`, "snapshot flag on populated day", "narration mirrors the previewed line", "greeting line comes first, preview as tail", "no name → byte-identical pre-R26 greeting on a populated day" (x2), "greeting path makes zero outbound calls". NOTE: after single-item block we deleted tasks; the e2e block re-adds "finish report" due today — but reminder 'stand up' was cancelled in the previous section and identity was reset? The e2e section sets userName Alex warm again but does NOT reset userModel; the time fact 'I work at night' may still be present → snapshot text would contain 'night today'. ALSO cancelled reminder with fired=true still counts in remindersToday. Fix: in the e2e block, add userModel.resetForTesting() and clear store (deleteTask all / cancelReminder) before re-adding, OR adjust expected strings.
- After e2e: 25th label = no-outbound checks (2 await calls) + completion log "All Round 26 greeting-briefing tests passed (${pass})."

## Current debugging state (as of latest run)
Harness first section: ALL 24 PASS (24 PASS lines confirmed; harness exit was via the e2e section).
E2E section error: `ReferenceError: greetSnapshot is not defined` — caused by the local `function greetLine(personality, userName)` defined at the BOTTOM of the file (lines 197+) which Node's hoisting evaluates... actually the error is 'greetSnapshot is not defined' — meaning some line references greetSnapshot bare. The local greetLine at bottom was written as a COPY of dispatcher's greetLine but possibly references greetSnapshot OR the test expects greetLine(...) but the harness's own greetLine exists at bottom only — check bottom of file. Wait the ACTUAL error: 'greetSnapshot is not defined' at line 169 — line 169 is `assert(!r0.detail.snapshot, ...)` — the IDENTIFIER is `r0.detail.snapshot` which can't throw that. Hmm — maybe the real culprit: the harness defines local `greetLine()` at bottom but inside e2e I call `greetLine("warm", "")` — that works if defined. The error 'greetSnapshot is not defined' is thrown because... dispatch-personal.js module exports greetSnapshot? Check module.exports at bottom of dispatch-personal.js — perhaps it exports greetLine from dispatch? Simplest: just check what the bottom of harness defines and what dispatch-personal exports.
Plan to fix e2e section:
1. identity.set({userName:'Alex',personality:'warm'}); userModel.resetForTesting(); also clear store (loop deleteTask on all tasks, cancelReminder all) — previous sections left 'finish report' undone + cancelled 'stand up' reminder counting
2. 'hey nova' greeting: snapshot text depends on reminder fired state — after clearing reminders it's ' — today has 1 thing on the plate.' (warm)
3. local greetLine helper at bottom must match dispatcher's EXACT wording (concise/professional/warm/playful) — it currently does (copied). OK.
4. no-outbound section needs fetch stub set BEFORE the await calls — currently stub + asserts live in a plain {} block inside the IIFE; fine, but fetch must not have been set earlier (earlier sections don't stub it). NOTE: globalThis.fetch stub persists across sections — the third section sets it; the second e2e block then uses it fine. BUT third section does fetch stub after e2e — order is e2e first, then no-outbound. Fine.

## STATUS UPDATE (phase 2)
- Harness DONE: 33 PASS, "All Round 26 greeting-briefing tests passed (33)."
- Wired: test:greeting-briefing added via script; .nova-greeting-test-data/ in .gitignore
- FULL CHAIN: EXIT 0, 855 PASS, 25 suites (822+33)
- REMAINING: README — insert "## Greeting-triggered day preview (Round 26)" section BEFORE "## Identity-aware briefing (Round 25)" (line ~295 area, before "## Nova identity (Round 24)" at 297); update R25 section chain footnote '24 suites, 822 tests' → '25 suites, 855 tests'; update feature table? optional. Then git add -A, commit, push, report, continue.
- README R25 section location: grep -n '## Nova identity (Round 24)' → 297; identity table row ~line 37 (grep '^| Nova identity')

## To do
1. Fix e2e section expectations/state isolation (reset userModel + clear store, or expect 'night today')
2. Run full harness → all pass with completion line
3. python3 scripts/add-test-script.py test:greeting-briefing "node src/main/test-greeting-briefing.js"
4. grep -qx ".nova-greeting-test-data/" .gitignore || echo >> .gitignore
5. npm test → EXIT 0; count = 822 + 25(ish) = 847
6. README: "## Greeting-triggered briefing (Round 26)" section BEFORE "## Identity-aware briefing (Round 25)"; update footnote on Round 25 section chain count to 24 suites 822; feature table? maybe skip (greeting preview is enhancement of identity row already)
7. git commit -m "Round 26: greeting-triggered day preview — \"good morning\" now opens with your plate (counts, all 4 personalities, time-of-day fact bridge, additive when no name/empty day), plus 9 new question phrasings (\"how's my day looking\", \"what's the plan for today\") routing to the daily-briefing gate; 25 tests; 25 suites, 847 tests" && git push
8. Report + start Round 27

## Round 27+ candidates
- Focus mode / Pomodoro timer with task tracking ("Nova, focus mode for 25 minutes")
- Smart task prioritization by voice ("what should I work on first?")
- github-gem-seeker: open-source integrations for Nova
- manim-animator: animated architecture explainer
- internet-skill-finder: trending AI assistant features
- Mood/energy check-in stored in user model, affects briefing tone

# Round 27 design (mood/energy check-in) — WORK IN PROGRESS

Design (lightweight, local-only, no model):
1. plan.js: new `RE_MOOD_CHECK` BEFORE identity ask branch: /^(?:nova\s*,?\s*)?(?:how am i feeling today|how am i doing|how do i feel|check in with me|what's my mood|what is my mood today)\s*$/i + extraction regex `RE_MOOD` for explicit statements "i feel ..." / "i'm feeling ...".
   - Simplest robust scope: one action `notes:mood-check` (no payload) that reads last mood; and "i feel X"/"i'm feeling X" piggybacks on RE_REMEMBER_FACT (it already starts with "remember..."? NO — "i feel tired" lacks "remember". Add separate RE_MOOD_STATEMENT: /^(?:nova\s*,?\s*)?(?:i feel|i'm feeling|i am feeling|i've been feeling|i've been)\s+.+\S\s*$/i → notes:mood-statement {mood}. Also "today i feel X" variant.
2. actions.js: register notes:mood-check L1, notes:mood-statement L1 (both SAFE — store only).
3. user-model.js: NO new API — mood statements are just facts ("I feel energetic today") added via addFact(); mood-check reads the most recent fact matching MOOD_FACT_REGEX (feel|feeling|feels|tired|energized|exhausted|stressed|great|down|off|amazing|meh|burned out).
4. dispatch.js: mood-check case → returns {ok:true, text:"You told me: \"…\" 2 hours ago. I've noted it in your briefing.", narration}, last mood + age ("just now"/"X hours ago"); if none → "You haven't told me yet — try \"I feel energetic today\"."
5. dispatch-personal.js: extend userFacts/personalization so briefing/digest/greeting lead-in gets a mood-aware prefix when a same-day mood exists: "You sounded energized this morning — here's your day: …" (additive). Only the SAME DAY's last mood counts (mood is ephemeral).
6. Harness test-mood-check.js (~30 tests): routing 6 phrases + negatives, statement extraction 5 phrases, dispatcher wording no-mood/recent/aged, additive rules (greeting unaffected when no mood, briefing lead-in with mood), persistence, zero outbound.
7. Note: greeting already carries snapshot; mood prefix should sit in greetSnapshot? Keep separate: mood prefix in the greeting only when mood present AND name? No — mood prefix is independent of name. greetSnapshot returns ""; greeting text = greetLine + preview + (mood prefix?). Order: greeting + moodPrefix(personality, mood) + preview. Additive both.
Chain after: 26 suites, ~885 tests.

## R27 PROGRESS (update 2)
- plan.js DONE: RE_MOOD_CHECK + MOOD_LEXICON + RE_MOOD_STATEMENT routed BEFORE greet (mood check lines after RE_GREETING block, before RE_NOTE). Order in file: RE_GREETING case → mood-check → mood-statement → RE_NOTE. All 9 question routes + statement routes + negatives PASS.
- actions.js DONE: notes:mood-check L1 + notes:mood-statement L1 registered.
- dispatch-personal.js DONE: latestMood(), moodAge(updatedAt, now), moodNarration(now) → "You mentioned ${age} that \"${fact}\" — ", moodGreet(now) → personality variants: concise `(${age}: "${fact}") — `, professional `I have your ${age} check-in on file ("${fact}") — `, playful `the cosmos heard you ${age} ("${fact}") — `, warm `you told me ${age} ("${fact}") — `. Exports added.
- dispatch.js DONE: briefing narration = personalizeNarration(...).replace(/^(\s*)/, m => m + moodNarration()); same for digest; greet narration = greeting + moodGreet() + preview (text unchanged). Cases notes:mood-check (no mood → "You haven't told me..." with suggestion; personality ack: concise "Latest check-in: ... (recorded X ago)", professional "Here's your latest check-in:", playful "The cosmos remembers:", warm "You told me, X:") and notes:mood-statement (detail.ok===false error text; ack: concise "Noted.", professional "Logged — I'll keep it in mind for your briefings.", playful "The cosmos heard you! ✨", warm "Got it — I'll keep that in mind today.") + exports latestMood/moodAge/moodNarration/moodGreet.
- Harness test-mood-check.js: 25 routing asserts PASS. REMAINING BUG: require("../permissions/action-registry") wrong path — harness is in src/main so it should be "./permissions/action-registry". Fix that, then re-run.
- Harness design: NOW=2026-08-18T10:00Z; setStorePathForTesting/identity.resetForTesting/userModels.resetForTesting("/tmp/.nova-mood-test-data"); identity.set({userName:'Alex',personality:'warm'}). Sections: routing done; e2e registry check → fix path; mood-check empty; add mood-statement fact + patch updatedAt to NOW-20min; check-in 20 minutes ago; moodAge assertions; personality acks (switch identity.set personality); latest-mood math (remember-fact 'I feel calm now' patch updatedAt NOW-2min, forget restores); weaving b1/d1/g1 with mood; additive b2/d2/g2 after forget; no-outbound fetch stub.
- After harness green: add-test-script.py test:mood-check; .nova-mood-test-data/ to .gitignore; full chain (expect 26 suites ~885 tests); README insert "## Mood/energy check-in (Round 27)" before "## Greeting-triggered day preview (Round 26)" section (grep: line ~293); update chain footnotes 25/855 → 26/885; commit+push; report; next round.
- README R27 section content: two actions (mood-check L1 read latest mood with age; mood-statement L1 stores fact with dedicated ack), MOOD_LEXICON gating avoids swallowing "I am a developer" notes, weaving into briefing/digest/greeting narration only (text untouched), additive guarantee, zero outbound, harness covers routing/negatives/age math/personality acks/latest-wins/forget-revert/additive/no-outbound.

## R27 DEBUG FINDINGS (update 3)
- moodAge math confirmed: minutes = floor(ms/60000); <2 → just now; <60 → minutes; <12h → hours; else day-part. For "1 minute ago" use 65000-119999ms window (115000 OK: floor(115/60)=1 → "1 minute ago" — but DBG showed "just now"?? floor(115000/60000)=1.9→1. Should NOT be just now. DBG output said "just now" — SUSPICIOUS: maybe module cached old code? No — code right. WAIT: DBG uses NOW2=2026-08-19T08:00Z minus 115000 → ms=115000 → minutes=1 → "1 minute ago". DBG printed "just now"?! Unless... the DBG line executed with NOW2 different? floor(115/60)=1 <60 → returns "1 minute ago". The only way to get "just now" is minutes<2 → minutes must be 0 or 1 → 1<2 → YES 1<2 is TRUE → just now! threshold is minutes < 2 → "1 minute ago" (1 min) falls into <2 → "just now". Code treats 1-minute-old as just now. So my test expectation wrong again: minutes<2 includes 1 minute. Fix expectation: "moodAge: 1 minute ago" → expect "just now"; or change to test 5 minutes ago (use 300000ms). Simplest: use 300000 (5min) → expect "5 minutes ago".
- b1.narration DBG shows mood IS woven: 'You mentioned just now that "I feel energized today" — Alex, here's your day: Nothing on the plate today — clear skies. Remembered about you: I feel energized today.' — my assertion looked for "I feel calm now" but the FORGET of 'I feel calm now' happened AFTER the brief... actually order: add 'I feel calm now' (ts in model), then assertions... DBG shows "I feel energized today"! Because latestMood picks newest: 'I feel calm now' was added at line 161, then forget at line 165 ('forget that I feel calm now'), then b1 briefing at 183 — calm was forgotten, energized remains! So harness flow bug: weaving tests run AFTER the forget. Move weaving block BEFORE the latest-mood math section (swap sections 2c and 3). Then assertions for 'I feel calm now' valid.
- After swap: latest-mood math section must re-add 'I feel calm now' for its assertions (it already does at line ~161, fine after swap).
