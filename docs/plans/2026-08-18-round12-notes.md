## Loop state (post Round 14, current date Aug 18 2026)

Rounds 10–14 COMPLETE and pushed to master (repo Prathamesh404NotFound/nova-voice-assistant):
- R10 78e6e43 command palette (Cmd/Ctrl+K, 11 tests)
- R11 7ffd3fe orb theme picker (4 themes, 9 tests)
- R12 6582b0b screenshot-to-note (27 tests)
- R13 40d6e20 reminder snooze (30 tests)
- R14 17aa872 task stats (23 tests) + 47516c3 gitignore

NOW: Round 15 — smart command-palette ranking (usage-based scoring).

## Round 15 scope
Smart ranking for the command palette (`src/renderer/js/command-palette.js` + `palette-setup.js`):
- Score = recency (halving every 24h) + frequency (sqrt), localStorage-backed
- Renderer-only like palette; persist under `nova-palette-usage` in localStorage
- Record a "run" whenever a palette item executes: palette-setup's run handler or
  command-palette's runItem — hook where `actionFn()` is called
- Harness: `src/main/test-palette-ranking.js` (JSDOM-free, like others): scoring
  math, recency decay, freq sqrt, cap, persistence, tie-break alphabetical
- Wire `test:palette-ranking` into package.json chain; README section + table row

## Key API facts (verified in R14)
- plan.js RE_TASK_STATS at top of planNoteAction (after RE_SCREEN_NOTE).
  Gotcha: optional-group + \b regex breaks under JS backtracking — use explicit
  alternation; lookbehind (?<!\b(?:add|mark)\b\s+.*) excludes create-phrasings.
- store.all() returns DISCONNECTED copies (stripInternal map) — harness mutations
  must persistAndReload: mutate snapshot → fs.writeFileSync(store.filePath()) →
  store.setStorePathForTesting(store.filePath()) (resets memory + __loaded=false,
  keeps file). In-memory mutation only NEVER takes effect.
- store.js exports: all, addNote, addReminder, rearmReminder, addTask, setTaskDone
  (now records completedAt, cleared on un-done), deleteNote, deleteTask,
  cancelReminder, searchNotes, summarizeOf, dueReminders, markFired,
  setStorePathForTesting, resetForTesting (deletes file!), filePath, taskStats.
- settings.js: getSetting/setSetting, setPrivateMode (no ForTesting; that name is
  wrong — use setPrivateMode), private mode blocks KB + outbound.
- gate.js executeAndLog returns {outcome, detail}; buffer stripped from LOG entry
  only when forNotes payload requested (execute includes buffer in detail).
- action-registry: registerAction({id, level, description, execute, simulate}),
  unregisterActionForTesting, listActions.
- dispatcher formatLocalResult: case per notes:* actionId; actions.js registerAction
  per notes:*.
- Test harness pattern: Module shim for 'electron' via Module._resolveFilename +
  _load override; globalThis.fetch blocked; DATA_DIR temp dir + gitignore prefix
  `.nova-<name>-test-data/`.

## Palette architecture (R10, to extend in R15)
- command-palette.js: IIFE, window.NovaCommandPalette. fuzzy match (prefix+substring),
  keyboard nav, HTML-escaped highlight, level chips.
- palette-setup.js: IIFE, window.NovaPaletteSetup. bindPaletteHotkey(openFn),
  createPalette({ startListening, stopSpeaking, isSpeaking, ... }).
  Catalog: L0 UI affordances + async main-process actions. Run handler executes the
  item's actionFn — that's the hook point for usage recording.

## Round 15+ candidates
- Smart palette ranking (R15 now)
- github-gem-seeker: find OS integrations
- content-gap-analysis: re-run vs competitors
- history export (chat transcript MD/PDF), smart search history, task due dates
