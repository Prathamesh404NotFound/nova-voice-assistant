## Round 14 — Task stats (IN PROGRESS)
ROOT CAUSE FOUND: store.all() returns DISCONNECTED copies (stripInternal map) — mutating them does nothing to __data or disk. ALL harness disk-surgery must: mutate the snapshot, fs.writeFileSync(store.filePath(), JSON.stringify(snapshot)), then store.setStorePathForTesting(store.filePath()) (resets __data + __loaded=false while keeping the file), then access triggers load() from disk. In-memory only mutations NEVER take effect — that's why streak tests got 1/0 wrong: the yesterday mutation was lost.
DEBUG LOG: '^task stats(?:istics)?\b' false on 'task statistics' (reproduced twice, minimal /statistics\b/ next). Root cause hypothesis: 'task statistics' input string ends after 's' — \b at end after word char matches. If still false, actual cause: my regex source line 61 has 'stats(?:istics)?' but perhaps with a hidden non-breaking char — od -c showed 0 non-ASCII though. Test /statistics\b/ on 'statistics' to isolate.

Voice: "how am I doing on my tasks" / "task stats" / "my task completion rate" → compute: total tasks, done, pending, completion rate %, current streak (consecutive days w/ ≥1 completion), week completions. L0/L1 safe local read; store needs stats helper (keep updatedAt on setTaskDone; store.tasks have done/updatedAt; completedAt unknown so streak ≈ days w/ done flip, best effort). action id "notes:task-stats" L1; add to plan.js RE_TASK_STATS before list rules; dispatch case; harness tests. Then round 15+ candidates: smart palette usage ranking, github-gem-seeker, content-gap-analysis re-run.

## Round 13 — Reminders snooze (COMPLETE, commit 40d6e20)
Voice: "snooze 10 minutes" → re-arm the LAST FIRED reminder (fired===true, most recent dueAt? actually store has markFired setting fired=true; snooze targets the fired reminder with dueAt nearest-past) via store.rearmReminder(id, newDueAt); action id "notes:snooze-reminder", L1 SAFE, simulate + plain narration "Reminder snoozed 10 minutes — I'll nudge you again at HH:MM". Add RE_SNOOZE regex to plan.js BEFORE RE_REMIND_CANCEL, dispatch formatLocalResult case "notes:snooze-reminder", tests in test-notes.js scope (notes intent routing via planner — can extend test-notes.js or new harness; prefer extending existing test-notes.js flow where planner+dispatcher are tested). Key APIs: store.rearmReminder(id, dueAt) at store.js:118; plan.js parseTime(expr) handles relative durations ("in 10 minutes", "10 minutes", "10 min"); reminders.fireReminder uses notifier; fired reminders have fired=true in store.

---

# Round 12 — Screenshot-to-note (COMPLETE, commit 6582b0b)

## Feature
"Nova, note what's on my screen" → capture (vision gate L0, {forNotes:true} to ferry PNG buffer) → offline OCR → save timestamped note tagged `[screen]` (notes:screen-to-note, L1 SAFE). Fully local, works in Private Mode (no network anywhere in path).

## Files created/modified (Round 12)
- `src/main/notes/screenshot-note.js` — NEW. Registers notes:screen-to-note action; execute: gate runAction("vision:capture-screen",{forNotes:true}) → ocr.recognizeText(buffer,{minConf:55}) → uses confidentText||text → store.addNote(`[screen] ...\n\n<ocr>`). Returns {ok,kind,note,charCount,durationMs,privateMode}.
- `src/main/vision/vision-actions.js` — execute() now returns detail.buffer when payload.forNotes.
- `src/main/permissions/gate.js` — executeAndLog strips detail.buffer before actionLog.append (Buffer.isBuffer check).
- `src/main/notes/plan.js` — NEW RE_SCREEN_NOTE regex checked BEFORE RE_NOTE in planNoteAction → {actionId:"notes:screen-to-note",payload:{}}. Regex: `^(?:nova\s*,?\s*)?(?:(?:note|save|capture|remember|write down)\s+(?:(?:what(?:'s| is)\s+)?(?:(?:on\s+)?(?:my|the)\s+)?screen))|(?:capture|snap)\s+(?:my\s+|the\s+)?screen(?:\s+and\s+(?:note|save)\s+it)?$` (tested; matches note what is on my screen / note my screen / capture the screen / save my screen / write down what is on my screen / snap the screen and note it; does NOT match note that I have a meeting / what is on my screen).
- `src/main/notes/dispatch.js` — formatLocalResult case "notes:screen-to-note" (ok true: preview first line + charCount; ok false: detail.error).
- `src/main/main.js` — `require("./notes/screenshot-note")` after notes/actions.

## Key APIs learned
- gate.runAction returns {outcome:"success"|"failed"|"cancelled"|"blocked", detail}; on success detail = execute() return.
- screenshot.captureScreen() → {buffer, width, height, permissionMissing, status, sourceId}.
- ocr.recognizeText(buf,{minConf}) → {text, words, confidentText}.
- notes store: all, addNote(text) → note{id,text,createdAt}, addReminder, addTask, setTaskDone, deleteNote, deleteTask, cancelReminder, searchNotes, summarizeOf.
- plan.js exports: planNoteAction(text, ctx), planNoteActionSafe is wrapper in classifier.
- classifier: planNoteActionSafe + noteAction.intent NOTES; screen phrasing "what is on my screen" routes VISION via VISION_RE (RE not matched here because RE_SCREEN_NOTE requires verb).

## Remaining Round 12 steps
1. Write src/main/test-screenshot-note.js harness (mock screenshot/ocr/store/settings via module cache — see test-vision.js style, 5-7 tests: planning regex, action registration+level, no-network/Private Mode note content never sent, buffer strip in log, formatting).
2. `npm run test:screenshot-note` green → add to package.json "test" chain.
3. `npm test` EXIT 0.
4. README: feature row (after "Local notes & reminders" row, line ~28) + `## Screenshot-to-note (Round 12)` section (place before "## Orb theme picker (Round 11)", line 276).
5. git add -A; commit "Round 12: screenshot-to-note ..."; git push; report; start next round.

## Test chain scripts (package.json)
test:permissions test:vision test:control test:agent test:files test:notes test:kb test:automation test:event-triggers test:memory test:command-palette test:themes
→ add "test:screenshot-note": "node src/main/test-screenshot-note.js" and chain `&& npm run test:screenshot-note` (before test:command-palette keeps alphabetical-ish; actually insert anywhere in chain).

## Commit history
- Round 10: 78e6e43 (command palette, 11 tests)
- Round 11: 7ffd3fe (orb theme picker, 9 tests)
- Loop rule: implement → npm test EXIT 0 → commit+push → report → next round immediately.

## Next round candidates (R13+)
- Reminders snooze ("snooze 10 minutes")
- Task stats panel (completion rate, streaks)
- Smart palette usage ranking
- github-gem-seeker integration check
- content-gap-analysis re-run vs VoltSetu-style competitors
