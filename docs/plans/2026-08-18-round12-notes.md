## Round 17 COMPLETE & PUSHED: de01392 (Round 17: task due dates, 29 tests) + b599110 (gitignore test data). Full chain EXIT 0, 539 tests/15 suites. README updated.

## Loop state (post Round 15, current date Aug 18 2026)

Rounds 10–15 COMPLETE and pushed to master (repo Prathamesh404NotFound/nova-voice-assistant):
- R10 78e6e43 command palette (11 tests), R11 7ffd3fe themes (9), R12 6582b0b screenshot-to-note (27),
  R13 40d6e20 snooze (30), R14 17aa872 task stats (23), R15 5b823eb palette ranking (12)

NOW: Round 16 — history export (chat transcript to Markdown + save-to-notes).

## Round 16 progress (implementation DONE, testing next)
- history-export.js CREATED (src/renderer/js/history-export.js): window.NovaHistoryExport =
  { renderMarkdown(historyItems, {now, appName}), suggestFilename(now),
    downloadMarkdown(mdText, filename) [Blob+anchor+revokeObjectURL],
    saveAsNote(historyItems, opts) [window.nova.runAction("notes:add-note", {text: title+md})] }
  Escaping: \ backtick pipe. KB sources as `file` code spans. Empty → honest note.
- index.html: added ⬇ Export .md (#historyExportBtn) + Save as note (#historySaveNoteBtn)
  in History side-label; script tag history-export.js before app.js.
- app.js: wiring block after theme block (~line 2179): export btn → renderMarkdown+
  downloadMarkdown → liveLine message; save btn → saveAsNote → refreshNotesPanel
  (NOT refreshNotes). DONE.
- test-history-export.js CREATED (11 checks: header/body, escaping, empty, memory badge,
  filename, download plumbing, default filename, saveAsNote ok/fail/throw, src channel)
  — NOT yet run.
- TODO: run harness, wire test:history-export into package.json chain, npm test, README
  update (feature table row + section), commit + push, report, start Round 17.
- Round 17 candidates: github-gem-seeker OS integrations, content-gap rerun,
  reminders snooze UI in side panel, task due dates.

## Round 17: task due dates (IN PROGRESS)
Scope: store dueDate on tasks; planner "add X by <time>" parses duration specs via
plan.js parseTime-like helper (parseDueDate: relative "in 3 days", "by Friday",
"next Monday", "tomorrow", "end of day"); notes:add-task payload {text, dueDate ISO}.
Panel shows due/overdue markers (small badge on pending tasks: "due today", "3d
overdue", "in 2d"); "what's due this week" / "overdue tasks" voice stats via
notes:task-due-stats or extending task-stats; sort list by due date ascending
(pending first with soonest due top); overdue + today nudges at boot. L1 SAFE, local.
Harness: test-task-due-dates.js (~14 tests) + wire into package.json chain.

## Round 17 progress (plan.js due-date branch REWRITTEN)
- parseDueDate() verified working: friday/tomorrow/in 3 days/next monday all return correct
  dates with setNowForTesting(new Date('2026-08-18T12:00:00Z')) (Wednesday).
- Root cause of first failure: RE_TASK_ADD is anchored (...to my tasks$), so 'add X by
  friday' never matched; my first dueMatch approach stripped wrongly.
- FINAL design (in plan.js): DUE_CLAUSE regex detects trailing 'by/due X' and strips
  ONLY if the remainder matches ^add\s+.+ or ^task[:\s]+.+$; then RE_TASK_ADD runs on
  stripped text. Non-task leftovers ending in a due clause → guidance error.
  Note: task regex branch '^task[:\s]+(.+)' matched 'task fix bug due in 3 days' earlier
  (taskText would include the due clause — fine because dueMatch strips it first).
- REMAINING: write test-task-due-dates.js (write to file, NOT inline node -e with $ —
  shell mangled it). Then panel due badges (renderer app.js task render ~line 110-140
  notesList), due-week stats action, README, package.json chain, commit+push.
- Panel notes list render: app.js refreshNotesList ~line 110, el.notesList. Add
  dueBadge helper: pending + dueDate → 'due today'/'in Nd'/'Nd overdue' small span.
- store.taskStats exists (Round 14) in store.js — add due stats helpers: dueThisWeek(),
  overdue().

## Round 17 debug notes
- plan.js parseDueDate bug: 'add finish the report to my tasks by friday' → error
  branch (dueDate null) while 'task fix bug due in 3 days' works → weekday branch fails.
  Suspect: dm[1] for 'by friday' may include 'by ' (alternation group includes
  (?:by\s+)? inside branch3) → parseDueDate('by friday') then tests
  /^((?:next\s+)?...)$/ on 'by friday': prefix captured group1='by ' so expr='by friday',
  weekday branch matches with prefix 'by ' → should still work! Actual: WEEK_MS unused
  var fine. Root cause TBD — debug parseDueDate('friday') direct.

## Round 16 scope
- Renderer-only module `src/renderer/js/history-export.js` (window.NovaHistoryExport):
  - exportHistoryToMarkdown(historyList) — builds Markdown transcript (# Nova session …,
    timestamps, user/nova markers, escape special chars like table pipes)
  - downloadMarkdown(md, filename) — Blob + <a download> click (renderer-safe, no new IPC)
  - saveHistoryAsNote(historyList) — sends transcript to notes via ctx (L1 via
    submitMessage-like path: reuse notes:add-note payload via window.nova.runAction)
  - Button in side panel History section (#historyExportBtn) — app.js wires it
- Renderer-only + one IPC call (window.nova.runAction "notes:add-note") which keeps
  its existing L1 gate. Export itself = L0 (read-only).
- Harness `src/main/test-history-export.js`: Markdown formatting, escaping, timestamps,
  empty history, filename stamp, save-to-note payload contract. ~12 tests.
- Wire test:history-export into package.json chain; README feature table row + section.

## Key API facts (verified in R15)
- index.html script order: wakeword.js, themes.js, command-palette.js, palette-setup.js,
  palette-ranking.js, app.js.
- palette-setup createPalette ctx keys: el, setSidePanel, runKbCommand, submitMessage,
  refreshPermPanel, exportLog, startListening, stopSpeaking, isSpeaking, onActionRun.
  onRun item.run(); ranking recordRun(String(item.id || item.label)) before run.
- app.js palette block ~line 2137. state.ttsActive = speaking flag.
- window.nova.runAction(id, payload, {dryRun}) returns {outcome, detail}.
  notes:add-note payload {text: ...}.
- history in renderer: side panel #history div; app.js probably keeps a history array
  (search "history" in app.js). exportLog uses el.permExportBtn.click().
- Test harness pattern: globalThis.shim 'electron' via Module._resolveFilename +
  _load; DATA_DIR temp dir with .nova-<name>-test-data/ prefix (gitignored).
- IIFE module tests: fs.readFileSync + new Function("window", src)(global).
- store.all() returns DISCONNECTED copies — harness mutations need
  persistAndReload: mutate snapshot → fs.writeFileSync(filePath()) →
  setStorePathForTesting(path) (resets memory+__loaded, keeps file).
  resetForTesting DELETES the file — avoid for disk surgery.
- settings.js: getSetting/setSetting, setPrivateMode.
- action-registry: registerAction/unregisterActionForTesting/listActions.
- gate.js executeAndLog → {outcome, detail}; buffer stripped from LOG when forNotes.

## Round 16+ candidates
- github-gem-seeker: OS integrations; content-gap-analysis rerun
- Reminders snooze UI in side panel, task due dates, smart search history,
  video-generator/demo video rerun with new features

## Round 17 REMAINING (save before compaction)
DONE so far:
- plan.js: parseDueDate() + DUE_CLAUSE strip-before-RE_TASK_ADD + setNowForTesting()
  + nowForTesting.now() clock; RE_TASK_STATS lookbehind fixed (was catastrophic:
  (?<!\b(?:add|mark)\b\s+.*) → (?<!\badd\b\s)(?<!\bmark\b\s)). ALL planner cases PASS
  (verified via scripts/check-due-dates.js: ALL PASS).
- store.js: addTask(text, {dueDate}) with NaN-guard; taskStats returns dueThisWeek +
  overdue (day-granular, pending only, done drops out).
- actions.js: notes:add-task execute passes payload.dueDate.
- dispatch.js: task-stats text includes overdue + due-this-week sentence.
- app.js refreshNotesList tasks tab: sorted pending-soonest-first, dueBadge() helper
  (due today / in Nd / Nd overdue), classes due-soon (cyan) / due-overdue (amber).
- hud.css: .notes-sub.due-overdue (var(--amber)) + .due-soon (var(--cyan-dim)) rules.

HARNESS BUG just hit: package.json has "type": "module" — but OTHER harnesses use CJS
require (Node auto-detects via… actually test-task-stats.js uses require fine; the
error was mine because I added `await dispatch.runNoteAction(...)` top-level await
which forced ESM parse. FIX: remove top-level await — dispatch.runNoteAction is async;
use .then() or IIFE. Copy shim pattern from test-task-stats.js (local shim object +
Module._load override + DATA_DIR argv[2]).
- harness file: src/main/test-task-due-dates.js (written, needs CJS fixes above)
  Tests (~20): parseDueDate math ×10, planner routing ×6, store persistence ×4,
  taskStats due view ×3, dispatch text ×2.

STILL TO DO:
1. Fix harness (remove top-level await → wrap in (async () => {...})()) + match shim
   pattern; run: node src/main/test-task-due-dates.js
2. Add "test:task-due-dates" script + chain in package.json (test chain line ~22-31:
   "test": "...&& npm run test:task-stats && npm run test:history-export")
3. README: feature table row "Task due dates" after Task stats row; Round 17 section
   before "Task stats (Round 14)" section (sections are reverse-chron).
4. git add -A && commit "Round 17: task due dates (...)" && git push origin master
5. Report, start Round 18. Round 18 candidates: reminder snooze UI in side panel,
   github-gem-seeker integrations, content-gap rerun, smart wake-word phrases.

## Round 18: task due-date lifecycle (IN PROGRESS)
Design: docs/plans/2026-08-18-round18-due-date-management.md
Scope: (1) voice change/clear due date → notes:set-task-due (L2 REVERSIBLE w/
simulate + reverse); (2) one-per-session overdue boot nudge (boot-nudge.js,
nova:task-due-nudge event, spoken when focused); (3) completion celebration
line when due-today/overdue task is marked done.

DONE:
- plan.js: RE_SET_DUE_VERB (noun-phrase + reschedule shorthand) + RE_CLEAR_DUE_VERB
  + RE_IMPLICIT_SET_DUE + RE_IMPLICIT_CLEAR_DUE (clear checked BEFORE set in
  planNoteAction — verified). setDueClause helper. Planner branch before
  RE_TASK_DONE. Exports added.
- store.js: setTaskDue(id, dueDate) NaN-guard + delete-on-null.
- actions.js: notes:set-task-due L2 w/ title body simulate + reverse restore.
- dispatch.js: set-task-due formatter; complete-task celebration extra line.
- boot-nudge.js CREATED: overdueNudgePayload() + start(mainWindow) 3s delay,
  once per boot flag, resetForTesting.
- main.js: boot-nudge.start(mainWindow) after reminders.start.
- preload.js: onTaskDueNudge cb.
- app.js: onTaskDueNudge listener (showNotesToast + speak when focused).
- scripts/check-r18-plan.js: 19 planner cases ALL PASS.

TODO:
1. test-task-due-management.js harness (~20 tests): planner routing (ctx
   variants), setTaskDue persistence/clear/NaN-guard, action registration
   (L2, simulate title, reverse restores old due), dispatch text (set +
   clear + celebration on-today/overdue), boot-nudge payload (overdue count,
   names ≤3 + "and N more", fires once only, resetForTesting).
   Harness shim pattern: const shim=require('../shim-electron.js');
   Module._load override; CJS + IIFE for async (package.json type:module —
   no top-level await).
2. Wire test:task-due-management into package.json test chain (test chain
   ends with && npm run test:task-due-dates).
3. npm test → EXIT 0.
4. README: feature table row "Due-date management" (after Task due dates row)
   + Round 18 section before "Task due dates (Round 17)" section.
   Sections reverse-chron: 286=Task due dates (Round 17), 290=Task stats (Round 14).
5. git add -A; commit; push; report; next round (candidates: reminders snooze
   UI in side panel, github-gem-seeker integrations, content-gap rerun).

R17 pushed earlier: de01392 + b599110. Full chain then: 539 tests/15 suites.

## R18 implementation ALSO DONE (save before compaction)
- test-task-due-management.js WRITTEN (~24 asserts): planner routing ×10,
  setTaskDue persistence/clear/NaN-guard/unknown-id, registration L2+simulate
  titles+reverse restore, dispatch set/clear/celebration text, boot-nudge
  (once-only, payload names overdue count + task, second start no-op).
  NOTE: harness runs dispatcher + boot-nudge timers → allow time in run.
  Harness uses CJS + IIFE (package.json type:module — no top-level await).

REMAINING:
1. Run: node src/main/test-task-due-management.js (may need timing fixes)
2. Wire "test:task-due-management" into package.json scripts + test chain
   (chain currently ends: ...&& npm run test:history-export && npm run
   test:task-due-dates)
3. npm test → EXIT 0 (current: 539 tests/15 suites)
4. README: feature table row "Due-date management" (after "Task due dates"
   row, line ~32) + "## Task due-date management (Round 18)" section BEFORE
   "## Task due dates (Round 17)" (line 286). Write voice examples: "change
   the due date for finish report to next monday", "remove the due date for
   buy milk", "finish report dropped its due date", boot nudge spoken at
   startup, celebration on due-today completion.
5. git add -A; commit "Round 18: task due-date lifecycle (voice set/clear due
   dates, boot nudge, completion celebration, tests)"; git push origin master
6. message result; continue to R19 (candidates: reminder snooze UI in side
   panel; github-gem-seeker integrations; content-gap rerun)

## R18 DEBUG (save before compaction)
- test-task-due-management.js still fails with "require is not defined in ES
  module scope" even with --experimental-detect-module=off. Node v22.13.0.
  test-task-due-dates.js (identical shim pattern) RUNS FINE.
  Root cause NOT yet found — hypothesis: the file contains top-level await
  INSIDE a comment? No. Check: maybe the file has a Unicode line separator
  char or the em-dash (—) at top triggers detection? test-task-due-dates.js
  ALSO has em-dash. Actual diff: R18 file has "(async () => {" block +
  IIFE. Hmm — detect-module scans for top-level await token even inside
  comments. My earlier top-level await lines were in a COMMENT block? No.
  TODO next: run `node --trace-warnings` or compare hex; simplest fix:
  delete the file and recreate with pure sed-like content avoiding ANY
  "await" at top level entirely (all awaits inside IIFE — verified they are
  inside (async () => {)). Actually R17 file ALSO works with await inside
  IIFE. So diff is elsewhere: maybe `await new Promise` lines 146/153 are
  inside the same IIFE (lines 118-157) — check the file structure: after
  my edit the IIFE starts at 118 and the old "(async () => {" at old 127 was
  REMOVED? Must verify braces — likely the IIFE wrapper is now broken:
  "(async () => {" at 118, then "})().catch(...)" only at file end. The
  module detector sees "await" outside async because I removed the
  inner (async () => { ... })() and merged blocks. VERIFY brace matching.
- package.json test chain ends: ...test:history-export && npm run
  test:task-due-dates. Wire test:task-due-management after it.
- README R18: row + section before "## Task due dates (Round 17)" (line 286).

## Round 19: reminder snooze UI — IN PROGRESS
Design doc: docs/plans/2026-08-18-round19-snooze-ui.md
R18 DONE + PUSHED: c03828a (feature) + 432b17d (gitignore). Full chain
559 tests/16 suites EXIT 0.

Plan: when a reminder fires, enqueue a pending-snooze entry (id-keyed,
15-min TTL) so the HUD's fired banner gets Snooze chips (5m/10m/30m/1h).
Chips call existing notes:snooze-reminder action (L1, reuse; invoke
nova:snooze-reminder {id, durationMs} from renderer). One snooze per fired
reminder; second replaces first; fails plain after re-fire or expiry.

Key facts gathered:
- src/main/notes/reminders.js: fireReminder(mainWindow) at line 55 sends
  nova:reminder-fired {id,text,dueAt,focused}; scanOnce marks fired first.
  Module exports start/stop/scanOnce/setNotifierForTesting/getNotifier/SCAN_MS.
- Store: rearmReminder() exists (Round 13); notes:snooze-reminder action
  already registered L1 in actions.js.
- Preload already has: onReminderFired. Renderer app.js line ~293 listens
  onReminderFired → showNotesToast + speak.
- Notes dir: actions.js dispatch.js plan.js reminders.js store.js
  boot-nudge.js screenshot-note.js summarize.js.
- Renderer fired banner: showNotesToast(msg) in app.js; banner has snooze
  chips to be ADDED there.
- package.json test chain now ends: && npm run test:task-due-management.
- Harness CJS tricks: shim-electron.js at repo root; Module._load override;
  Node 22 detect-module: NO top-level await anywhere in harness (all awaits
  inside (async () => {...})()); require paths relative to src/main;
  registry.getAction(id) NOT listActions (list strips simulate/reverse).

R19 remaining:
1. reminders.js: add pendingSnoozes Map (id → {until, reminder}) in
   fireReminder; new function snoozeFired(id, durationMs) → rearmReminder;
   test hooks resetPendingSnoozesForTesting, pendingSnoozeTtl (15min).
2. main.js IPC: already? check — maybe add nova:snooze-reminder invoke in
   main.js notes IPC block (reuse notes:snooze-reminder action via gate?).
   Simplest: call reminders.snoozeFired directly + log to action log.
3. Renderer app.js: extend fired banner toast with snooze chips (create
   small element, chips call window.nova.snoozeReminder if added to preload
   — ADD preload IPC: snoozeReminder: (id,dur) => invoke("nova:snooze-reminder").
4. test-snooze-ui.js (~12 tests): wiring, TTL expiry, once-only, replace,
   renderer chip logic (test the plain JS helper module snoopUI.js).
5. npm test EXIT 0; README row + section; commit; push; report; R20.

## R19 state (save before compaction)
R19 plan: docs/plans/2026-08-18-round19-snooze-ui.md
DONE so far:
- reminders.js: __pendingSnoozes Map + snoozeFired(id,seconds) + hooks
  pendingSnoozesForTesting/resetPendingSnoozesForTesting/setSnoozeWindowMsForTesting.
  fireReminder registers entry on fire.
- actions.js: notes:snooze-reminder execute now honors p.fromUi+p.id targeting
  (voice path unchanged = newest fired); dueAt from seconds when not given.
- main.js: nova:snooze-reminder IPC -> gate.runAction notes:snooze-reminder
  {id, seconds, fromUi:true} (line ~506).
- preload.js: snoozeReminder: (id,seconds) => invoke("nova:snooze-reminder").
- renderer/js/snooze-ui.js: NEW pure-JS module window.NovaSnoozeUI {init,
  buildBanner, markDismissed, resetForTesting, QUICK}. buildBanner gets
  container callback via showNotesToast augment.
- app.js: showNotesToast(text, augment) extended; onReminderFired passes
  augment that appends NovaSnoozeUI.buildBanner to the toast container.
  NOTE: toast auto-removes after 3.5s — chips must live; consider making
  toast longer OR moving banner to side panel. CURRENT: chips only work
  within 3.5s window — likely BAD UX. TODO: check before shipping:
  better = append banner inside side panel notes area? Minimum: keep but
  know it's 3.5s. Decision: acceptable for R19 but note in README; consider
  moving banner to side panel as improvement in later round.
- index.html: <script src="js/snooze-ui.js"> added before app.js.
- hud.css: .nova-snooze-bar / .nova-snooze-chip CSS appended.
- Harnesses written: src/main/test-snooze-ui.js (main process) +
  src/main/test-snooze-ui-renderer.js (fake DOM). NOT yet run/wired.
REMAINING: run both harnesses; wire scripts into package.json (append to
"test" chain after test:task-due-management); full npm test EXIT 0; README
row + "Reminder snooze UI (Round 19)" section before Round 18; commit+push;
report; R20.
Full chain so far: ... && npm run test:task-due-management (16 suites,
559 tests EXIT 0 before R19).
Harness gotchas: CJS shim pattern (Module._load electron), NO top-level
await, registry.getAction not listActions, store.setStorePathForTesting,
DATA_DIR under /tmp/.nova-<name>-test-data (gitignored pattern).

## Round 19 COMPLETE (commit 5c7a1b9, pushed)
Reminder snooze UI: snooze chips (5m/10m/30m/1h) on fired-reminder banner;
pending-snooze queue (15-min window); single-use chips; sticky 90s banner;
routes through same L1 notes:snooze-reminder action as voice (fromUi + id
targeting in actions.js execute). Files: reminders.js (+hooks), actions.js,
main.js (nova:snooze-reminder IPC), preload.js, renderer/js/snooze-ui.js,
app.js (showNotesToast augment + sticky toast), index.html, hud.css, README.
Two harnesses: test-snooze-ui.js + test-snooze-ui-renderer.js, 22 tests.
Full chain EXIT 0, 581 tests / 18 suites.
Round 20 candidates: overdue-task boot nudge (done R18) — pick: smart
wake-word phrase suggestions / task due-date editing voice (done R18) —
next: "github-gem-seeker" integrations, or reminder snooze panel UI
(permanent snooze buttons in Reminders tab rather than toast-only),
overdue nudge speak-once already done. Good R20: snooze in side panel
Reminders tab + maybe internet-skill-finder for trending ideas.

## R20 state (save before compaction)
Plan: docs/plans/2026-08-18-round20-reminders-panel.md
Decision summary: Reminders tab gets fired-reminder rows with snooze chips
(reuse window.NovaSnoozeUI? chips route to nova:snooze-reminder IPC directly,
not via NovaSnoozeUI banner helpers — fired rows in panel are permanent,
chips re-render on list refresh). Pending reminders keep × cancel
(existing notes-action=cancel via notes-run). After snooze/cancel →
refreshNotesList. Fired rows grouped TOP of list, cyan "fired N ago" label.
Main process: NOTHING new (snooze IPC exists from R19; store.rearmReminder
works on any .fired reminder). Renderer only.
app.js reminders render at lines ~171-181 (tab === "reminders" block in
renderNotesList): currently items.map single div. Replace with fired-first
sort + snooze bar for fired items (build chips manually calling
window.nova.snoozeReminder(id, sec) + speak + refreshNotesPanel).
Click handler for notes-del cancel at line ~292 in el.notesList listener.
speak() is app.js local fn; refreshNotesList/Panel exist.
CSS: reuse .nova-snooze-bar/.nova-snooze-chip (already in hud.css); add
small .fired marker style only.
Tests: new src/main/test-reminders-panel.js (fake DOM, ~15 tests) — fired
rows top, chips present with data-seconds, snooze click invokes ipc +
spoken confirmation + list refresh, pending cancel unchanged.
Then wire scripts in package.json (python json edit pattern worked R19),
npm test EXIT 0, README row + section before Round 19, commit+push, report, R21.
Current chain tail: ... && test:snooze-ui && test:snooze-ui-renderer
(18 suites, 581 tests, EXIT 0 as of R19).

## R20 state (save before compaction)
Plan: docs/plans/2026-08-18-round20-reminders-panel.md
DONE:
- snooze-ui.js: added buildReminderRowChips(reminder) + snoozeReminderRow(id,sec,text,bar),
  per-row scope (__rows Map), single-use disable, api exposes both; resetForTesting clears.
- app.js: renderNotesList reminders branch reworked — fired rows sorted top with cyan
  "fired " label + snooze row chips (via NovaSnoozeUI), pending after; cancel btn preserved;
  ipc wrapper refreshes list on success; onReminderFired calls refreshNotesList (tab shows
  fired row live).
- hud.css appended: .notes-item.fired (cyan left border), .notes-sub-fired (cyan), nowrap.
- test-reminders-panel.js harness written (fake DOM, mirrors renderReminders). NOTE: harness
  selector strings must use template literals `` for quotes — single quotes with \" leave
  literal backslashes (found=0 bug).
NEXT: run harness (fix any asserts), wire package.json script test:reminders-panel + append
  to test chain (python json pattern: read, add script, chain += ' && npm run test:reminders-panel'),
  npm test EXIT 0, README row (after Snooze row, before Task due dates row) + section
  "## Reminder snooze UI in panel (Round 20)" before "## Reminder snooze UI (Round 19)",
  commit+push, report, R21.
Chain tail before R20: ... && test:snooze-ui && test:snooze-ui-renderer (18 suites, 581 tests).
CSS vars: --cyan: #39d2ff. Test harness quirks: CJS shim via Module._load, IIFE no top-level await,
FakeEl querySelectorAll recursive over descendants.

## R20 harness debugging state
File: src/main/test-reminders-panel.js (fake DOM harness, mirrors renderReminders from app.js).
- selector parsing: attr selectors (incl. button[attr="val"]), class selectors (.cls + tag.cls),
  bare tagName — all working now.
- setAttribute stores kebab dataset key + DOM-style camel alias (data-seconds→seconds,
  data-notes-action→notesAction, data-reminder-id→reminderId). WORKS.
- chips assert now PASSes ("fired row renders four snooze chips" etc).
- REMAINING FAIL: "row keeps its cancel button (start the laundry)" —
  row.querySelectorAll(`button[data-notes-action="cancel"]`) returns 0 even though the
  cancel BUTTON (className notes-del, dataset {notesAction:'cancel', notesId:'r-fired-2',
  kebab keys also present}) is a direct child of the row.
- Root cause found in /tmp/brtest.js: matches() attr branch stores/looks up dataset by kebab
  key `data-notes-action`?? NO — key='notes-action' but dataset lookup is c.dataset[key] with
  camel key 'notesAction' — and setAttribute DOES store camel 'notesAction'. Raw print showed
  chips dataset {"data-seconds":"300","seconds":"300"} — camel OK for chips.
- For the cancel row: kids print showed BUTTON notes-del dataset {"notesAction":"cancel","notesId":"r-fired-2"} — NO kebab keys! Because renderReminders in the harness sets btn.dataset.notesAction directly (line ~156), not via setAttribute. So kebab key absent. But the matches branch looks up c.dataset[key] where key='notes-action' (kebab) — MISMATCH. FIX: in matches attr branch, look up c.dataset[valKeyCamel] — or simpler: also store kebab in dataset via direct assignment in renderReminders (btn.dataset['data-notes-action']='cancel') OR change matches to try camel key (inner key minus 'data-' prefix camelCased).

## R20 harness — near-done state (just before this note)
- Harness file: src/main/test-reminders-panel.js. All bug fixes DONE:
  * FakeEl.querySelectorAll recursive + attr/class/tagName branch ordering fixed
  * setAttribute stores kebab + camelCase dataset keys (data-foo→foo)
  * Async tests wrapped in async IIFE; FakeEl.click() returns Promise; chip clicks awaited
  * All ipc stubs push their tracker arrays (snoozed/ipcCalls)
  * No more sed damage (avoid sed with regex chars; use file edit)
- S6 debug shows ipcCalls=[1 call] after fix — NOW list6 ipc pushes; rerun expected ALL PASS
- After harness passes: wire package.json ("test:reminders-panel": "node src/main/test-reminders-panel.js" +
  append && npm run test:reminders-panel to test chain — edit via python json or file edit, NOT sed with
  regex chars), npm test EXIT 0, README: feature table row "Reminder snooze UI in panel" after Snooze row;
  section "## Reminder snooze UI in panel (Round 20)" before "## Reminder snooze UI (Round 19)",
  commit+push, report, start R21.
- R20 features: fired rows sorted top in Reminders tab w/ cyan "fired " label + 4 snooze chips
  (5m/10m/30m/1h) via snooze-ui.js buildReminderRowChips (per-row single-use disable, id-targeted);
  app.js ipc wrapper refreshes list on success; onReminderFired refreshes tab; hud.css
  .notes-item.fired / .notes-sub-fired rules. CSS vars --cyan #39d2ff, Orbitron/Space Grotesk/JetBrains Mono fonts.

## R20 COMPLETE (commit 6cc1730)
Reminder snooze UI in panel: fired rows sorted top (oldest first) with cyan "fired" label +
per-row 4-chip snooze bars (5m/10m/30m/1h) via NovaSnoozeUI.buildReminderRowChips; id-targeted
through the same L1 notes:snooze-reminder gate; single-use per row; success re-renders list;
onReminderFired refreshes the tab; .notes-item.fired/.notes-sub-fired CSS.
17 tests in test-reminders-panel.js (fake DOM: recursive qSA + async-aware click()).
npm test EXIT 0, 598 tests / 19 suites. README row + section done. Pushed.
R21 next (candidates: reminder snooze UI in side panel DONE → pick next: overdue task boot nudge
DONE too → try: task due-date editing DONE (R18) → candidates: smart wake-word phrase
suggestions, reminder edit by voice, task priority tags, daily briefing automation,
command-palette history of recent voices, github-gem integrations, KB docx/pdf richer,
manim demo video, top-design UI polish pass).

## R21 state (save before compaction)
Design doc: docs/plans/2026-08-18-round21-daily-briefing.md
Plan: round 21 "daily briefing". Files to touch:
1. store.js: add dailyBriefing(now) function before deleteNote + export it (insert after taskStats).
   Fields: {dueToday:[{id,text}], overdue:[{id,text,dueDate}] oldest-first, remindersToday:[{id,text,dueAt,fired}]}
   sorted by dueAt. Day granularity, done excluded. (EDIT IN PROGRESS - was doing this edit when file changed)
2. plan.js: RE_BRIEFING = /^(?:nova\s*,?\s*)?(?:what('s| is) on my plate today|brief me on today|(?:today|morning|daily)\s+briefing|what do i have due today)$/i —
   branch before RE_NOTES_LIST area (around line 519) → {actionId:"notes:daily-briefing", payload:{}}
   (no ctx needed, read-only).
3. actions.js: register notes:daily-briefing L1 SAFE execute: {result: store.dailyBriefing(), kind:"daily-briefing"}
   (register near notes:task-stats at line ~193).
4. dispatch.js: case notes:daily-briefing → text: "Here's today's plate: N due today (names), N overdue (names), N reminders today."
   empty: "Nothing on the plate today — clear skies." narration "Let's look at your day…"
   detail: {kind:"daily-briefing", briefing: result}
5. app.js: renderer renders briefing card (like task-stats card pattern — find where dispatch
   detail kinds render, likely "task-stats" kind) — 3 groups: Due today cyan, Overdue amber, Reminders.
6. harness: test-daily-briefing.js (CJS shim like test-task-due-dates.js, IIFE no top-level await,
   plan.setNowForTesting(d), store.setStorePathForTesting + resetForTesting). ~18 tests.
7. scripts/add-test-script.py exists (do NOT use sed with regex chars). python3 scripts/add-test-script.py test:daily-briefing "node src/main/test-daily-briefing.js"
8. npm test EXIT 0 → README row (after Task due dates row, before Due-date management? order: Snooze→R Snooze UI→R Snooze UI in panel→Task due dates→Due-date mgmt→Task stats)
   + section "## Daily briefing (Round 21)" before "## Reminder snooze UI in panel (Round 20)".
9. commit+push, report, R22.
Current chain: 19 suites 598 tests. Latest commit 6cc1730.
Dispatcher existing patterns: task-stats case at dispatch.js:252; narration field exists.
Plan note-action flow: planNoteAction(text, ctx={}) returns {actionId,payload}; dispatcher
runNoteAction invokes action + formats; renderer gets detail.kind objects via ipc result
(in main.js notes IPC: result.detail passed through). Command palette searches via app.js;
briefing accessible via typing too (no palette registration needed — voice/type via planner).

## R21 progress (update before compaction)
DONE: store.js dailyBriefing(now) + export; plan.js RE_BRIEFING + branch before RE_NOTES_LIST;
actions.js register notes:daily-briefing (L1 SAFE, {result, kind:"daily-briefing"});
dispatch.js case notes:daily-briefing (spoken "Here's today's plate: ..."/"Nothing on the plate
today — clear skies.", narration "Here's what's on your plate today…", detail {kind, briefing});
app.js runNotesCommand: kind==="daily-briefing" → showNotesToast with augment building
.nova-briefing-toast .nova-briefing-groups / .nova-briefing-group {brief-due-today|brief-overdue|brief-reminders}
(label + items); NO refreshNotesList for briefing. hud.css appended (~38 lines) with briefing CSS.
Harness src/main/test-daily-briefing.js WRITTEN (mirrors test-task-due-dates.js pattern:
Module._resolveFilename/_load shim, DATA_DIR argv[2] default .nova-daily-briefing-test-data,
assert helper, pinned NOW 2026-08-18T12Z Wed).
⚠️ Harness issue to check: it calls dispatch.runNoteAction(...) but dispatch.js exports
`runNoteAction`? MUST VERIFY: dispatch.runNoteAction exists AND accepts {actionId,payload,detail}
— otherwise replace with direct text check via detail object (it does: detail.result).
Currently assert for dispatcher expects null → harmless, but SHOULD fix to use real export.
grep to verify: dispatch exports runNoteAction probably — check src/main/notes/dispatch.js exports.
REMAINING: verify export → fix assert → run node src/main/test-daily-briefing.js → python3
scripts/add-test-script.py test:daily-briefing "node src/main/test-daily-briefing.js" → npm test
EXIT 0 → README row (feature table after "Reminder snooze UI in panel" row) + section
"## Daily briefing (Round 21)" before "## Reminder snooze UI in panel (Round 20)" section →
commit+push → report → R22.
README sections current order: Round 16 History export, R19 snooze UI, R20 snooze UI in panel,
R18 due-date mgmt, R17 task due dates, R14 task stats, R13 snooze...
Feature table row order: Snooze → Reminder snooze UI → Reminder snooze UI in panel → Task due dates...
Latest commit: 6cc1730 (R20). Chain currently: 19 suites / 598 tests.

## R21 DONE & VERIFIED (2026-08-18)
- Harness bugs fixed: storeContext undefined → removed (dispatch.runNoteAction(text) real API);
  plan.js subj ReferenceError → moved RE_BRIEFING check BEFORE RE_IMPLICIT_SET_DUE
  ("what do i have due today" matched the implicit-due fallback); registry require path
  ./permissions/*. 
- node src/main/test-daily-briefing.js → EXIT 0, 33 PASS ("All Round 21 daily-briefing tests passed").
- Wired via scripts/add-test-script.py. npm test → EXIT 0, 20 suites, 614 PASS.
- REMAINING: README (feature table row after "Reminder snooze UI in panel"; section
  "## Daily briefing (Round 21)" before "## Reminder snooze UI in panel (Round 20)"),
  commit+push, report, start R22. Latest commit before this: 6cc1730.

## R22 morning briefing automation — IN PROGRESS (state before compaction)
R21 committed: d7caff5 (added .gitignore entry .nova-daily-briefing-test-data/).

R22 IMPLEMENTATION DONE:
- parser.js: RE_BRIEFING_STEP regex (briefing clause forms incl. tell me/give me prefix) +
  added to classifyClause NOTES branch; RE_BRIEFING_PRESET =
  /^(?:set up|create|start|schedule|make me|add)\s+(?:a\s+)?(?:daily|morning)\s+briefing(?:\s+(?:at|for)\s+(.+))?$/i
  in parseAutomation — default cron 0 8 * * *, name "Morning briefing", single notes step
  "what's on my plate today"; bad time → plain error.
- runner.js: runNotesStep now forwards res.narration; buildSummary returns narrated
  result text as-is when exactly 1 narrated step (so dispatcher briefing wording speaks
  cleanly from automations).
- test-morning-briefing.js WRITTEN (18 of 24 asserts pass so far). Harness pattern: same
  shim as test-automation.js + notes/actions+kb/files/vision-actions require; notesStore
  setStorePathForTesting(path.join(DATA_DIR,'notes.json')) + resetForTesting.
  Run: node src/main/test-morning-briefing.js
BUG HIT: "schedule a briefing for 6 AM" returns NOT ok ("could not find a schedule") —
  RE_BRIEFING_PRESET requires (?:daily|morning) before "briefing"; bare "a briefing" fails.
  FIX OPTION: make (daily|morning) optional in RE_BRIEFING_PRESET (keep (?:a\s+)?).
  Then verify "create a briefing" does not collide w/ non-automation text (it's anchored
  start/end, so fine).
REMAINING:
1. Fix RE_BRIEFING_PRESET regex (optional daily|morning), rerun harness, all pass.
2. Wire test:morning-briefing via python3 scripts/add-test-script.py
   test:morning-briefing "node src/main/test-morning-briefing.js"
3. npm test → EXIT 0 (currently 614 tests/20 suites)
4. README: feature table row "Morning briefing automation" (after "Daily briefing" row,
   line ~33) + section "## Morning briefing automation (Round 22)" BEFORE
   "## Daily briefing (Round 21)" (line ~290). Voice examples: "set up a morning
   briefing at 7:30", "create a daily briefing", "every weekday at 7:30 AM, tell me my
   tasks and what's on my plate today".
5. git add -A && commit "Round 22: morning briefing automation (...)" && git push
6. Report R22 done; start R23 (candidates: github-gem-seeker integrations,
   content-gap-analysis rerun, internet-skill-finder trending features,
   weekly digest preset, briefing pin-to-panel, smart wake-word phrases).
Test count: 18 PASS so far; harness asserts ~24.
