# Round 34 state — smart recurring tasks & reminders (UPDATED: all green)

## Status: IMPLEMENTATION COMPLETE, tests green
- `npm test` → EXIT 0. 33 suites in chain. Harness `src/main/test-recurring.js` = 75 PASS assertions, no failures.
- Total suite count this round: 33 suites, ~1,355 PASS assertions across full chain (previous: 32 suites, 1,278).

## Files changed (all committed-ready, NOT yet pushed)
1. src/main/notes/plan.js — RE_RECURRING regex, parseRecurring, routing branch before RE_REMIND; plus NEW stop/remove-recurring route (mStop regex: "stop/remind/cancel/don't remind me to X", "remove my recurring X") using matchTask() against ctx.recurring.
2. src/main/notes/store.js — __data.recurring, addRecurring (arms reminder row at nextDue for reminder mode; task mode arms nothing), computeNextDue (daily/weekday/named-day/week=7d-cycle/month/half-day anchors 7:00/13:00/18:00/21:00 default 9:00), removeRecurring (active=false + cancelReminder row), getRecurring, requeueFired (after fire: advance nextDue, rearm same reminder row w/ fired=false + new dueAt; task mode → addTask new instance + advance nextDue). dueReminders now honors test clock via liveNow(). all() includes recurring. resetForTesting fixed (covers focus+recurring). stale duplicate resetForTesting deleted, comments fixed.
3. src/main/notes/reminders.js — scanOnce: after fireReminder(r) → store.requeueFired(r.id) re-arms recurring rows.
4. src/main/notes/actions.js — notes:add-recurring + notes:remove-recurring, L2 REVERSIBLE, simulate/execute/reverse.
5. src/main/notes/dispatch-personal.js — recurringConfirmText (weekday-name → "every Monday", time from item/ISO; additive user words) + recurringRemoveText.
6. src/main/notes/dispatch.js — cases notes:add-recurring / notes:remove-recurring; storeContext() now includes recurring.
7. src/main/test-recurring.js — NEW harness, 75 tests, 10 sections (routing positives/negatives, store, nextDue math, requeue, task-mode, dispatcher e2e incl. stop-route removal, registry L2, wording, additive + zero-outbound).
8. package.json — test:recurring wired at end of chain.
9. .gitignore — .nova-recurring-test-data/ added.

## Remaining steps
1. README.md: insert "## Smart recurring tasks (Round 34)" section BEFORE line 301 "## Topic note search (Round 33)". Update chain footnotes (32 suites/1,278 → 33 suites/~1,355 where stated).
2. Delete scripts/smoke-recurring.js and scripts/smoke-recurring-route.js.
3. git add -A && commit && push (master).
4. Report to user; start Round 35.

## Round 35+ candidates
- Voice-controlled settings ("set my personality to playful", "turn on private mode")
- github-gem-seeker: open-source integrations for Nova
- Focus stats visual in side panel
- manim-animator: animated architecture explainer video
- Smart note tagging / auto-categorization
- Location-aware reminders stub ("remind me when I get home")

## Gotchas learned this round
- harness body with top-level await flips Node 22 → ESM; wrap async parts in async IIFE + .catch.
- "weekly" cadence = 7-day cycle (next Wed from Wed), NOT next Monday.
- computeNextDue needs half-day anchors when item.time absent.
- dueReminders defaulted to live new Date(); test clock seam required.
- dispatch-personal cadenceWords was passed no `day` from dispatch.js — confirmation read "every monday" lowercase; fixed by threading item.day into the call.
- task-mode removal has no reminder row; test removed task-mode spec first then Monday-spec.
