# Round 36 state — Focus stats side-panel visual (UPDATED: harness green)

## FINAL verified numbers
- Harness: src/main/test-focus-stats-panel.js = 63 tests, EXIT 0.
- Full npm test chain: EXIT 0, 1,472 PASS lines (1,409 + 63), 38 chain entries → README says "35 suites, 1,472 tests, EXIT 0".
- test:focus-stats-panel wired in package.json; .nova-focus-stats-panel-test-data/ in .gitignore.

## Goal
"Focus stats in side panel" — the spoken focus-stats answers (Round 32) get a companion VISUAL: a fourth side-panel tab "Focus" showing a bar chart of focus minutes per day (today + trailing 6 days), a total row (today + 7-day), and a recent-sessions list. Fully local (L0-equivalent), no network.

## Key facts discovered (main codebase layout)
- Side panel tabs: `src/renderer/index.html` lines 148-150: three buttons `.notes-tab active` with `data-notes-tab="notes|tasks|reminders"`, inside the side panel (id sidePanel around line 140+).
- Tab switching/rendering: `src/renderer/js/app.js` lines 125-237: `NOTES_STATE.tab` ("notes"), `setNotesTab(tab)`, `refreshNotesList()` reads `window.nova.getNotesStore()` via `await`, tab→plural key `tab + "s"` → store.notes/tasks/reminders arrays. Empty messages dict at lines 170-174. Render add form at `renderNotesAddForm()` (lines 239-270) with per-tab form + `runNotesCommand(...)`.
- `getNotesStore`: preload `src/preload/preload.js` line 91 → ipcMain.handle `nova:get-notes-store` at main.js line 490 returns `{ok, store: notesStore.all()}` (store.all() includes focus/recurring per R34).
- Focus data: `src/main/notes/store.js` — `focusHistory(limit)` (line 699, newest first, stripInternal), `latestFocus`, `focusMinutesThisWeek(now)`, `focusMinutesToday(now)`. Sessions: {id, durationMin, startedAt, stoppedAt, status, cancelled} (real-elapsed math in focusMinutes*).
- Focus HUD chip exists in app.js (lines 74-100, `state.focusSession`), events nova:focus-started/stopped at preload lines 112-124.
- Existing harnesses: src/main/test-focus-stats.js (R32, dispatcher wording), test-focus.js (R29 store), src/renderer tests: test-snooze-ui.js (fake DOM, recursive querySelectorAll, async click()). snooze-ui.js is a headless-testable DOM helper pattern to mirror.
- Store resetForTesting includes focus collection. clockHm in plan.js formats h:mm AM/PM local.
- test harness pattern: env vars before require, Module._resolveFilename shim for electron, async IIFE, assert(), fetch blocker via overwriting globalThis.fetch.

## Design (Round 36)
1. New L0-equivalent read-only IPC `nova:get-focus-stats` in main.js → returns {ok, stats: {todayMin, weekMin, daily: [{date (ISO day), minutes}], recent: [{id, text?, durationMin, startedAt, status}]}}. daily = last 7 days (today-6..today), minutes = completed-session real-elapsed minutes sum per local day.
2. Preload exposes `getFocusStats: () => ipcRenderer.invoke("nova:get-focus-stats")`.
3. Renderer: fourth tab button `<button class="notes-tab" data-notes-tab="focus" role="tab">Focus</button>`; add "focus" case to emptyMsg dict; in refreshNotesList, render a pure-CSS/SVG bar chart helper (extracted to new file `src/renderer/js/focus-stats-ui.js`, DOM-agnostic like snooze-ui.js) + totals + recent sessions. Bars: today highlighted (cyan), other days (bg-elev bars), labels Mon/Tue/Wed or short date + minutes. New CSS tokens reuse --cyan/--bg-elev; add `.focus-chart` styles to hud.css.
4. Panel refresh on nova:focus-stopped event: refresh the Focus tab when open.
5. Harness: main-process `src/main/test-focus-stats-panel.js`? NO — store/stats are already tested in test-focus-stats.js; test the new helper: put the bar builder in `src/renderer/js/focus-stats-ui.js` + headless DOM harness `src/main/test-focus-stats-panel.js` mirroring snooze-ui harness (fake DOM, querySelectorAll, click handlers, bar count/height ordering, empty state, totals row, day labels). Add ~55 tests.
6. Wire: python3 scripts/add-test-script.py test:focus-stats-panel "node src/main/test-focus-stats-panel.js"; gitignore .nova-focus-stats-panel-test-data/ if needed.
7. npm test → EXIT 0; README insert "## Focus stats side panel (Round 36)" before Round 35; update footnotes to 35 suites, 1,464 tests (1,409+55).
8. Commit + push.

## Progress checklist
- [x] Phase 1: reviewed layout (above facts)
- [x] main.js IPC nova:get-focus-stats inserted at line 502 (after get-notes-store). Returns {ok, stats: {todayMin, weekMin, daily:[{day,label,minutes} x7, today last, label="Today"], recent: focusHistory(10)}}.
- [x] src/renderer/js/focus-stats-ui.js WRITTEN — exports {buildPanel, buildTotals, buildChart, buildRecent, fmtMin, elapsedMinutes}; DAY_LABELS; bar heights % of max (min 2px baseline); today col gets .focus-today; totals row shows empty-msg when week=0 and recent empty; recent capped 10 rows, cancelled rows labeled "(stopped early)"; fmtMin m<60 "X min", else "X h [Y min]".
- [ ] preload getFocusStats (src/preload/preload.js line 91 style: getFocusStats: () => ipcRenderer.invoke("nova:get-focus-stats"))
- [ ] index.html line 150: add <button class="notes-tab" data-notes-tab="focus" role="tab">Focus</button>
- [ ] app.js: NOTES_STATE.tab "notes"; add "focus" to emptyMsg dict (msg: "No focus sessions yet — say \“start focus mode\” to begin a Pomodoro…"); refreshNotesList() tab==="focus": call window.nova.getFocusStats(), if !store or !store.stats.daily → empty message; else append NovaFocusStatsUI.buildPanel(stats) (include script tag in index.html). notesAdd form: for focus tab → form "Start focus…" input + submit → runNotesCommand with duration (default empty → "start focus mode"). focus-stopped event refresh when tab open. Also load focus-stats-ui.js via <script> in index.html.
- [ ] hud.css: .focus-panel, .focus-totals, .focus-chart (flex row), .focus-bar-col, .focus-bar (cyan #39d2ff today else #2a2f42 bg-elev-ish), .focus-bar-label, .focus-bar-min, .focus-recent, .focus-recent-row, .focus-cancelled
- [ ] test harness: src/main/test-focus-stats-panel.js — fake DOM (mirror test-snooze-ui.js pattern: recursive querySelectorAll by tag/class/data-*, async click()), load focus-stats-ui.js via require with fake document object (UI uses global `document` — harness must provide global document before require). ~55 tests: totals wording (empty, today only, week, h-min formatting), chart bar count 7, today highlighted, relative heights, labels, recent rows, cancelled label, hour formatting, escapeHtml.
- [ ] wire: python3 scripts/add-test-script.py test:focus-stats-panel "node src/main/test-focus-stats-panel.js"; add .nova-focus-stats-panel-test-data/ to .gitignore
- [ ] npm test → EXIT 0; README insert "## Focus stats side panel (Round 36)" before Round 35; footnotes → 35 suites, ~1,464 tests (1,409+55)
- [ ] commit + push; report; start Round 37

## DONE so far (verified)
- [x] main.js IPC nova:get-focus-stats (line ~502). Returns {ok, stats:{todayMin,weekMin,daily:[{day,label:"Today"|null,minutes}x7 today-last],recent:focusHistory(10)}}.
- [x] preload getFocusStats added (line ~94).
- [x] index.html: Focus tab button at line 152; focus-stats-ui.js script tag at line 341 (after snooze-ui.js, before app.js).
- [x] focus-stats-ui.js written + node --check OK.
- [x] app.js edits: refreshNotesList focus branch (getFocusStats + buildPanel), emptyMsg.focus, renderNotesAddForm focus branch (form → start focus mode for N minutes, max 480, then refreshNotesList). NOTE: node --check on app.js itself PASSES?? — actually it FAILS at line 325 (runNotesCommand) BUT original HEAD also fails under my wrapper; MY PYTHON BALANCER shows balance -1 only at final `})();` for BOTH orig and current → file structure is balanced and fine; node --check failure on my sed-wrapped copy was my wrapper bug (I stripped `(() => {` at line 5 but file may contain another brace IIFE?). DEBUGGING STATUS (as of now):
- node --check on /tmp/orig.js (git show HEAD app.js) = OK.
- Current file FAILS at line 324: `async function runNotesCommand`.
- Acorn token comparison (brackets/parens/braces ONLY): cur vs orig diverge only at first edit point (my insertions), and token streams match 1:1 afterwards (no net bracket add/remove). So bracket structure is fine.
- Found + fixed one dup-const: `const res` in focus branch (line 167 declares outer res; renamed to fres). STILL FAILS.
- NEXT: the error must be a NON-bracket syntax issue in my inserted text. My insertions: (1) emptyMsg.focus line — contains a long string, ok. (2) focus branch try/catch — `try { ... } catch {}` fine. (3) focus branch in renderNotesAddForm — template literals with backticks fine.
- SUSPECT: my find/replace consumed or altered something ELSE near the reminders block tail: the ORIGINAL reminders block ended with `});` (no return). My replacement found text ending at `...runNotesCommand(`remind me to ${v}`);\n    });\n  }\n\n  /**` — that `}` in find WAS the function closer + it also consumed the blank line before the JSDoc. My replacement ended with `...runNotesCommand(\`remind me to ${v}\`);\n    });\n  }\n}\n\n  /**` — wait, that's what failed. Then I re-edited to: notes-if→return;}, tasks-if→return;}, FOCUS-if→return;}, reminders block (fall-through, ends `});`), `  }` function closer. That should be VALID. Yet still fails!
- POSSIBLY: somewhere in my replacement the backtick template literal broke: the find/replace strings I used contained backticks — the tool may have mangled them? Check lines 287-320 raw with cat -A for stray chars, and run acorn FULL parse (not just tokens) to get exact token error.
- RESOLVED: parse failure was (a) duplicate `const res` → renamed to `fres`; (b) an extra `  }` after the reminders fall-through block (leftover from earlier edits). app.js NOW PASSES node --check. focus-stats-ui.js, main.js, preload.js all OK.

## CURRENT TODO (exact next steps)
1. main.js handler at lines 502-528: FIX the minutes math — currently `stopped > to` skips sessions crossing midnight and `Math.min(planned, elapsed)` caps at planned; replace with started-in-day logic + real elapsed Math.round((stopped-started)/60000). Also consider: `stopped > to` condition means a session started 23:50 and stopped 00:10 skips — started-in-day fixes it.
2. Write src/main/test-focus-stats-panel.js (~55 tests): harness pattern — env vars before require, electron shim scripts/shim-electron.js via Module._resolveFilename, async IIFE (NO top-level await), assert(), store.setNowForTesting for clock pinning, store.focusHistory seeding (sessions {id, durationMin, startedAt, stoppedAt, status:"completed"}), resetForTesting + setStorePathForTesting, fetch blocker on globalThis.fetch. Sections: IPC handler returns daily[7] incl Today label; todayMin/weekMin; midnight-crossing session counts for started-day; cancelled sessions excluded; empty store; panel UI builder via fake document (global.document = fake with createElement stubs — mirror test-snooze-ui.js fakeDOM pattern); dispatcher words via focusStatsSummary (R32 already tested — skip); zero outbound; no user facts written; test-clock leak verification (dueReminders respects pin).
3. Wire: python3 scripts/add-test-script.py test:focus-stats-panel "node src/main/test-focus-stats-panel.js"; echo ".nova-focus-stats-panel-test-data/" >> .gitignore (check not already there)
4. npm test > /tmp/testlog36.txt 2>&1; echo EXIT $?
5. README: insert "## Focus stats side panel (Round 36)" section before Round 35; footnotes 34→35 suites, 1409+~55=~1464 tests, EXIT 0 (R35 section says 34 suites/1409; R34 33/1355; R33 32/1278).
6. git add -A (exclude 1/ artifact dir + .nova-* test data), commit with round message, push.
7. Report; start Round 37 candidates: smart note tagging/auto-categorization; "Remind me when I get home" location stub; focus stats animation; command palette for notes actions.

## Notes
- main.js IPC handler location: after nova:get-notes-store block (around line 502).
- focus-stats-ui.js exports: window.NovaFocusStatsUI = {buildPanel(stats)} on load (IIFE). Fake-DOM test requires document stubs used by UI: createElement, appendChild, innerHTML setter, classList, addEventListener, textContent. Check UI file for exact DOM API usage before writing harness.
- The app.js focus-tab refresh also on focus-stopped event is OPTIONAL (not implemented) — the Refresh button handles it; consider adding later.

## Key code facts (for harness)
- store.focusHistory(limit) returns sessions [{id, durationMin, startedAt, stoppedAt, status}] newest first; stripInternal; store.focusMinutesThisWeek() = completed real-elapsed minutes in trailing 7d.
- UI file uses `document` global; harness must set global.document = fake before require("../../renderer/js/focus-stats-ui").
- Focus events in main.js nova:notes-run: on success sends nova:focus-started / nova:focus-stopped — panel could refresh on stopped (optional).

## Reminder: prior rounds' conventions
- README footnote convention: chain suites = npm test entries - 3? NO — used literal chain length minus offset: R34 said 33 suites/1,355 for 36-entry chain; R35 said 34 suites/1,409 for 37 entries. Pattern: suites = chain entries - 3, tests = sum of harness PASS-line counts in chain (R35: 1,409 = 1,355+54). For R36: chain entries = 38 → suites = 35; tests = 1,409 + new harness PASS lines count.
- Commit message style: "Round N: ... — detail; detail; 54 tests; 34 suites, 1409 tests, EXIT 0"
- Add .nova-*-test-data/ to .gitignore for the new harness data dir.
- Top-level await flips Node 22 ESM — wrap harness in async IIFE with .catch → process.exit(1).
